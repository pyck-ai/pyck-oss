info:
  name: Zitadel Configuration
  type: http
  seq: 4

http:
  method: GET
  url: "{{env_zitadel_url}}/.well-known/openid-configuration"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
