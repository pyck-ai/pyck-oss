info:
  name: User - Group CRUD Lifecycle
  type: folder
  seq: 1

docs: |-
  Creates a group, verifies it appears in list and single-item queries, updates it, and deletes it. Skipped when the token lacks admin role.


request:
  auth: inherit
