info:
  name: User - Role CRUD Lifecycle
  type: folder
  seq: 1

docs: |-
  Creates a role, verifies it appears in list and single-item queries, updates it, and deletes it. Skipped when the token lacks admin role.


request:
  auth: inherit
