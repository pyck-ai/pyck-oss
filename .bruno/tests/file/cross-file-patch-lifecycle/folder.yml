info:
  name: Cross - File Patch Lifecycle
  type: folder
  seq: 1

docs: |-
  Tests JSON Patch (RFC 6902) operations on files. Creates a data type and file with data, updates via full overwrite, patches individual fields, verifies changes persist, then cleans up.


request:
  auth: inherit
