info:
  name: Cross - File Data Type Slug Reference
  type: folder
  seq: 1

docs: |-
  Creates a data type with a slug, then creates a file that references it via dataTypeSlug + custom data. Exercises the file service's data type cache (cache is populated via the NATS event emitted by createDataType).


request:
  auth: inherit
