info:
  name: Cross - Receiving Inbound Patch Lifecycle
  type: folder
  seq: 1

docs: |-
  Tests JSON Patch (RFC 6902) operations on receiving inbounds and inbound items. Creates a data type and inbound with data, patches individual fields, verifies changes persist, then cleans up.


request:
  auth: inherit
