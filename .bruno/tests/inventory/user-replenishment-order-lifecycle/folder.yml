info:
  name: User - Replenishment Order Lifecycle
  type: folder
  seq: 1

docs: |-
  Creates a replenishment order with items, reads it back, updates it, then deletes it. All steps after create are skipped when the create step returns errors.


request:
  auth: inherit
