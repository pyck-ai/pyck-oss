info:
  name: Cross - Inventory Item Patch Lifecycle
  type: folder
  seq: 1

docs: |-
  Tests JSON Patch (RFC 6902) operations on inventory items. Creates a data type and item with data, updates via full overwrite, patches individual fields, verifies both changes persist, then cleans up.


request:
  auth: inherit
