info:
  name: User - Workflow IsAssignable Lifecycle
  type: folder
  seq: 1

docs: |-
  Full toggle round-trip for the pyck_workflow_is_assignable search attribute, exercising the SDK's SetUpdateHandler end-to-end through the pyck gateway. Requires a tenant that already has at least one running workflow execution — the default seeded environment has none, so the suite only passes when run against a tenant with real workflow activity.


request:
  auth: inherit
