# Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen. DO NOT EDIT.
# Source: backend/workflow/api/testdata/examples/setworkflowisassignable.example.yaml

info:
  #name: Set Workflow Is Assignable - Set Workflow IsAssignable
  name: Set Workflow Is Assignable
  type: graphql
  seq: 3

docs: |-
  Toggles the assignability flag on a running workflow execution by sending a Temporal update. The value is persisted to the pyck_workflow_is_assignable search attribute, which controls whether the workflow appears in assignableWorkflowExecutions queries.


graphql:
  method: POST
  url: "{{env_baseurl_workflow_query}}"
  auth: inherit
  body:
    query: |-
      # Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen. DO NOT EDIT.
      # Source: backend/workflow/api/testdata/examples/setworkflowisassignable.example.yaml
      #
      # ┌──────────────────────────────────────────────────────────┐
      # │  (!) ACTION REQUIRED                                     │
      # │                                                          │
      # │  Open the Variables tab and replace every value          │
      # │  marked with // PLACEHOLDER before running this request. │
      # └──────────────────────────────────────────────────────────┘

      mutation SetWorkflowIsAssignable($input: SetWorkflowIsAssignableInput!) {
        setWorkflowIsAssignable(input: $input) {
          isAssignable
        }
      }
    variables: |-
      // Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen.
      // Source: backend/workflow/api/testdata/examples/setworkflowisassignable.example.yaml
      //
      // ┌──────────────────────────────────────────────────────────┐
      // │  (!) ACTION REQUIRED                                     │
      // │                                                          │
      // │  Replace every value marked with // PLACEHOLDER before   │
      // │  running this request.                                   │
      // └──────────────────────────────────────────────────────────┘

      {
        "input": {
          "isAssignable": false,
          "workflowExecutionID": null, // PLACEHOLDER: run ID
          "workflowID": null // PLACEHOLDER: workflow execution ID
        }
      }

runtime:
  scripts:
    - type: tests
      code: |-
        // Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen. DO NOT EDIT.
        // Source: backend/workflow/api/testdata/examples/setworkflowisassignable.example.yaml

        var body = res.body;
        var vars = req.body.variables;

        test("Status is 200", function() {
          expect(res.status).to.equal(200);
        });

        test("No GraphQL errors", function() {
          expect(body?.errors).to.not.exist;
        });

        test("Response echoes the new value", function() {
          expect(body?.data?.setWorkflowIsAssignable?.isAssignable).to.equal(vars.input.isAssignable);
        });

settings:
  encodeUrl: true
  timeout: 5000
  followRedirects: true
  maxRedirects: 5
