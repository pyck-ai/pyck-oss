# Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen. DO NOT EDIT.
# Source: backend/workflow/api/testdata/examples/ensuretemporalnamespace.example.yaml

info:
  #name: Ensure Temporal Namespace - Ensure Temporal Namespace
  name: Ensure Temporal Namespace
  type: graphql
  seq: 5

docs: |-
  Ensures the Temporal namespace exists for the current tenant. Safe to call multiple times. Returns the namespace name.


graphql:
  method: POST
  url: "{{env_baseurl_workflow_query}}"
  auth: inherit
  body:
    query: |-
      # Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen. DO NOT EDIT.
      # Source: backend/workflow/api/testdata/examples/ensuretemporalnamespace.example.yaml

      mutation EnsureTemporalNamespace {
        ensureTemporalNamespace
      }

runtime:
  scripts:
    - type: tests
      code: |-
        // Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen. DO NOT EDIT.
        // Source: backend/workflow/api/testdata/examples/ensuretemporalnamespace.example.yaml

        var body = res.body;

        test("Status is 200", function() {
          expect(res.status).to.equal(200);
        });

        test("No GraphQL errors", function() {
          expect(body?.errors).to.not.exist;
        });

        test("Namespace returned", function() {
          expect(body?.data?.ensureTemporalNamespace).to.exist;
        });

settings:
  encodeUrl: true
  timeout: 5000
  followRedirects: true
  maxRedirects: 5
