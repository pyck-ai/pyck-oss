# Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen. DO NOT EDIT.
# Source: backend/workflow/api/testdata/examples/cancelworkflow.example.yaml

info:
  #name: Cancel Workflow - Cancel Workflow
  name: Cancel Workflow
  type: graphql
  seq: 5

docs: |-
  Requests graceful cancellation of a running workflow execution. The target workflow receives a cancellation signal on its context and can run cleanup before terminating. Returns once the cancel request has been accepted by Temporal (not when the workflow has actually terminated).


graphql:
  method: POST
  url: "{{env_baseurl_workflow_query}}"
  auth: inherit
  body:
    query: |-
      # Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen. DO NOT EDIT.
      # Source: backend/workflow/api/testdata/examples/cancelworkflow.example.yaml
      #
      # ┌──────────────────────────────────────────────────────────┐
      # │  (!) ACTION REQUIRED                                     │
      # │                                                          │
      # │  Open the Variables tab and replace every value          │
      # │  marked with // PLACEHOLDER before running this request. │
      # └──────────────────────────────────────────────────────────┘

      mutation CancelWorkflow($input: CancelWorkflowInput!) {
        cancelWorkflow(input: $input) {
          workflowID
          workflowRunID
        }
      }
    variables: |-
      // Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen.
      // Source: backend/workflow/api/testdata/examples/cancelworkflow.example.yaml
      //
      // ┌──────────────────────────────────────────────────────────┐
      // │  (!) ACTION REQUIRED                                     │
      // │                                                          │
      // │  Replace every value marked with // PLACEHOLDER before   │
      // │  running this request.                                   │
      // └──────────────────────────────────────────────────────────┘

      {
        "input": {
          "workflowID": null, // PLACEHOLDER: workflow execution ID
          "workflowRunID": null // PLACEHOLDER: run ID
        }
      }

runtime:
  scripts:
    - type: tests
      code: |-
        // Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen. DO NOT EDIT.
        // Source: backend/workflow/api/testdata/examples/cancelworkflow.example.yaml

        var body = res.body;
        var vars = req.body.variables;

        test("Status is 200", function() {
          expect(res.status).to.equal(200);
        });

        test("No GraphQL errors", function() {
          expect(body?.errors).to.not.exist;
        });

        test("Workflow IDs echoed back", function() {
          expect(body?.data?.cancelWorkflow?.workflowID).to.equal(vars.input.workflowID);
          expect(body?.data?.cancelWorkflow?.workflowRunID).to.equal(vars.input.workflowRunID);
        });

settings:
  encodeUrl: true
  timeout: 5000
  followRedirects: true
  maxRedirects: 5
