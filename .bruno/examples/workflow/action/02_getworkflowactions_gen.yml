# Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen. DO NOT EDIT.
# Source: backend/workflow/api/testdata/examples/getworkflowactions.example.yaml

info:
  #name: Get Workflow Actions - Get Workflow Actions
  name: Get Workflow Actions
  type: graphql
  seq: 2

docs: |-
  Returns available queries and updates for a workflow execution, with an enabled flag indicating whether each action is currently callable.


graphql:
  method: POST
  url: "{{env_baseurl_workflow_query}}"
  auth: inherit
  body:
    query: |-
      # Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen. DO NOT EDIT.
      # Source: backend/workflow/api/testdata/examples/getworkflowactions.example.yaml
      #
      # ┌──────────────────────────────────────────────────────────┐
      # │  (!) ACTION REQUIRED                                     │
      # │                                                          │
      # │  Open the Variables tab and replace every value          │
      # │  marked with // PLACEHOLDER before running this request. │
      # └──────────────────────────────────────────────────────────┘

      query GetWorkflowActions($input: GetWorkflowActionsInput!, $where: WorkflowActionsWhereInput) {
        workflowActions(input: $input, where: $where) {
          queries {
            description
            enabled
            name
          }
          updates {
            description
            enabled
            name
          }
        }
      }
    variables: |-
      // Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen.
      // Source: backend/workflow/api/testdata/examples/getworkflowactions.example.yaml
      //
      // ┌──────────────────────────────────────────────────────────┐
      // │  (!) ACTION REQUIRED                                     │
      // │                                                          │
      // │  Replace every value marked with // PLACEHOLDER before   │
      // │  running this request.                                   │
      // └──────────────────────────────────────────────────────────┘

      {
        "input": {
          "workflowExecutionId": null, // PLACEHOLDER: run ID
          "workflowId": null // PLACEHOLDER: workflow execution ID
        },
        "where": {
          "enabled": true,
          "nameHasPrefix": "ship"
        }
      }

runtime:
  scripts:
    - type: tests
      code: |-
        // Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen. DO NOT EDIT.
        // Source: backend/workflow/api/testdata/examples/getworkflowactions.example.yaml

        var body = res.body;

        test("Status is 200", function() {
          expect(res.status).to.equal(200);
        });

        test("No GraphQL errors", function() {
          expect(body?.errors).to.not.exist;
        });

        test("Queries returned", function() {
          expect(body?.data?.workflowActions?.queries).to.exist;
        });

        test("Updates returned", function() {
          expect(body?.data?.workflowActions?.updates).to.exist;
        });

settings:
  encodeUrl: true
  timeout: 5000
  followRedirects: true
  maxRedirects: 5
