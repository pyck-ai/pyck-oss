# Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen. DO NOT EDIT.
# Source: backend/file/api/testdata/examples/createfile.example.yaml

info:
  #name: Create File - Create file
  name: Create File
  type: graphql
  seq: 1

docs: |-
  Creates a new file record linked to a supplier entity. Expects the server to return a file ID and a pre-signed upload URL.


graphql:
  method: POST
  url: "{{env_baseurl_file_query}}"
  auth: inherit
  body:
    query: |-
      # Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen. DO NOT EDIT.
      # Source: backend/file/api/testdata/examples/createfile.example.yaml
      #
      # ┌──────────────────────────────────────────────────────────┐
      # │  (!) ACTION REQUIRED                                     │
      # │                                                          │
      # │  Open the Variables tab and replace every value          │
      # │  marked with // PLACEHOLDER before running this request. │
      # └──────────────────────────────────────────────────────────┘

      mutation CreateFile($input: CreateFileInput!) {
        createFile(input: $input) {
          file {
            contentType
            createdAt
            createdBy
            data
            dataTypeID
            dataTypeSlug
            deletedAt
            deletedBy
            description
            id
            name
            publicAlias
            publicURL
            refid
            reftype
            size
            tenantID
            updatedAt
            updatedBy
            url
          }
          id
          preSignedUploadUrl
        }
      }
    variables: |-
      // Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen.
      // Source: backend/file/api/testdata/examples/createfile.example.yaml
      //
      // ┌──────────────────────────────────────────────────────────┐
      // │  (!) ACTION REQUIRED                                     │
      // │                                                          │
      // │  Replace every value marked with // PLACEHOLDER before   │
      // │  running this request.                                   │
      // └──────────────────────────────────────────────────────────┘

      {
        "input": {
          "contentType": "image/jpeg",
          "name": "product-photo.jpg",
          "refid": null, // PLACEHOLDER: entity ID (e.g. supplier ID)
          "reftype": "supplier",
          "size": 204800
        }
      }

runtime:
  scripts:
    - type: tests
      code: |-
        // Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen. DO NOT EDIT.
        // Source: backend/file/api/testdata/examples/createfile.example.yaml

        var body = res.body;

        test("Status is 200", function() {
          expect(res.status).to.equal(200);
        });

        test("No GraphQL errors", function() {
          expect(body?.errors).to.not.exist;
        });

        test("createFile response is complete", function() {
          expect(body?.data?.createFile).to.exist;
          expect(body?.data?.createFile?.id).to.exist;
          expect(body?.data?.createFile?.preSignedUploadUrl).to.exist;
        });

        test("File metadata matches input", function() {
          expect(body?.data?.createFile?.file?.id).to.exist;
          expect(body?.data?.createFile?.file?.name).to.equal('product-photo.jpg');
          expect(body?.data?.createFile?.file?.contentType).to.equal('image/jpeg');
          expect(body?.data?.createFile?.file?.size).to.equal(204800);
          expect(body?.data?.createFile?.file?.reftype).to.equal('supplier');
        });

settings:
  encodeUrl: true
  timeout: 5000
  followRedirects: true
  maxRedirects: 5
