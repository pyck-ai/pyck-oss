# Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen. DO NOT EDIT.
# Source: backend/management/api/testdata/examples/assignroles.example.yaml

info:
  #name: Assign Roles - Assign service roles to a user
  name: Assign Roles
  type: graphql
  seq: 5

docs: |-
  Grants one or more per-service roles to an existing user within a tenant. Additive — roles the user already holds are preserved. Only per-service role keys are accepted (ladder roles reader/writer/admin are rejected). Tenant-admin only.


graphql:
  method: POST
  url: "{{env_baseurl_management_query}}"
  auth: inherit
  body:
    query: |-
      # Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen. DO NOT EDIT.
      # Source: backend/management/api/testdata/examples/assignroles.example.yaml
      #
      # ┌──────────────────────────────────────────────────────────┐
      # │  (!) ACTION REQUIRED                                     │
      # │                                                          │
      # │  Open the Variables tab and replace every value          │
      # │  marked with // PLACEHOLDER before running this request. │
      # └──────────────────────────────────────────────────────────┘

      mutation AssignRoles($input: AssignRolesInput!) {
        assignRoles(input: $input) {
          roles
          userId
        }
      }
    variables: |-
      // Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen.
      // Source: backend/management/api/testdata/examples/assignroles.example.yaml
      //
      // ┌──────────────────────────────────────────────────────────┐
      // │  (!) ACTION REQUIRED                                     │
      // │                                                          │
      // │  Replace every value marked with // PLACEHOLDER before   │
      // │  running this request.                                   │
      // └──────────────────────────────────────────────────────────┘

      {
        "input": {
          "roles": [
            "inventory_service"
          ],
          "tenantId": null, // PLACEHOLDER: tenant UUID
          "userId": null // PLACEHOLDER: management user UUID
        }
      }

runtime:
  scripts:
    - type: tests
      code: |-
        // Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen. DO NOT EDIT.
        // Source: backend/management/api/testdata/examples/assignroles.example.yaml

        var body = res.body;

        test("Status is 200", function() {
          expect(res.status).to.equal(200);
        });

        test("No GraphQL errors", function() {
          expect(body?.errors).to.not.exist;
        });

        test("Returns the user's service roles after assignment", function() {
          expect(body?.data?.assignRoles?.userId).to.exist;
          expect(body?.data?.assignRoles?.roles).to.include('inventory_service');
        });

settings:
  encodeUrl: true
  timeout: 5000
  followRedirects: true
  maxRedirects: 5
