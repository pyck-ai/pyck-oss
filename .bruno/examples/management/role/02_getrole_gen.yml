# Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen. DO NOT EDIT.
# Source: backend/management/api/testdata/examples/getrole.example.yaml

info:
  #name: Get Role - Get Role
  name: Get Role
  type: graphql
  seq: 2

docs: |-
  Retrieves a single role by ID including its policies and users.


graphql:
  method: POST
  url: "{{env_baseurl_management_query}}"
  auth: inherit
  body:
    query: |-
      # Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen. DO NOT EDIT.
      # Source: backend/management/api/testdata/examples/getrole.example.yaml
      #
      # ┌──────────────────────────────────────────────────────────┐
      # │  (!) ACTION REQUIRED                                     │
      # │                                                          │
      # │  Open the Variables tab and replace every value          │
      # │  marked with // PLACEHOLDER before running this request. │
      # └──────────────────────────────────────────────────────────┘

      query GetRole($id: ID!) {
        role(id: $id) {
          createdAt
          createdBy
          deletedAt
          deletedBy
          description
          id
          name
          tenantID
          updatedAt
          updatedBy
        }
      }
    variables: |-
      // Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen.
      // Source: backend/management/api/testdata/examples/getrole.example.yaml
      //
      // ┌──────────────────────────────────────────────────────────┐
      // │  (!) ACTION REQUIRED                                     │
      // │                                                          │
      // │  Replace every value marked with // PLACEHOLDER before   │
      // │  running this request.                                   │
      // └──────────────────────────────────────────────────────────┘

      {
        "id": null // PLACEHOLDER: role ID
      }

runtime:
  scripts:
    - type: tests
      code: |-
        // Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen. DO NOT EDIT.
        // Source: backend/management/api/testdata/examples/getrole.example.yaml

        var body = res.body;

        test("Status is 200", function() {
          expect(res.status).to.equal(200);
        });

        test("No GraphQL errors", function() {
          expect(body?.errors).to.not.exist;
        });

        test("Role returned", function() {
          expect(body?.data?.role?.id).to.exist;
        });

settings:
  encodeUrl: true
  timeout: 5000
  followRedirects: true
  maxRedirects: 5
