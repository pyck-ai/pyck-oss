# Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen. DO NOT EDIT.
# Source: backend/management/api/testdata/examples/registertenant.example.yaml

info:
  #name: Register Tenant - Register Tenant
  name: Register Tenant
  type: graphql
  seq: 5

docs: |-
  Registers a new tenant. Requires admin privileges.


graphql:
  method: POST
  url: "{{env_baseurl_management_query}}"
  auth: inherit
  body:
    query: |-
      # Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen. DO NOT EDIT.
      # Source: backend/management/api/testdata/examples/registertenant.example.yaml
      #
      # ┌──────────────────────────────────────────────────────────┐
      # │  (!) ACTION REQUIRED                                     │
      # │                                                          │
      # │  Open the Variables tab and replace every value          │
      # │  marked with // PLACEHOLDER before running this request. │
      # └──────────────────────────────────────────────────────────┘

      mutation RegisterTenant($input: RegisterTenantInput!) {
        registerTenant(input: $input) {
          success
          tenant {
            createdAt
            createdBy
            data
            dataTypeID
            dataTypeSlug
            deletedAt
            deletedBy
            expiresAt
            id
            idpOrgRef
            name
            updatedAt
            updatedBy
          }
        }
      }
    variables: |-
      // Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen.
      // Source: backend/management/api/testdata/examples/registertenant.example.yaml
      //
      // ┌──────────────────────────────────────────────────────────┐
      // │  (!) ACTION REQUIRED                                     │
      // │                                                          │
      // │  Replace every value marked with // PLACEHOLDER before   │
      // │  running this request.                                   │
      // └──────────────────────────────────────────────────────────┘

      {
        "input": {
          "idpOrgRef": null, // PLACEHOLDER: IDP organization reference
          "name": null // PLACEHOLDER: tenant name
        }
      }

runtime:
  scripts:
    - type: tests
      code: |-
        // Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen. DO NOT EDIT.
        // Source: backend/management/api/testdata/examples/registertenant.example.yaml

        var body = res.body;
        var vars = req.body.variables;

        test("Status is 200", function() {
          expect(res.status).to.equal(200);
        });

        test("No GraphQL errors", function() {
          expect(body?.errors).to.not.exist;
        });

        test("Registration succeeded", function() {
          expect(body?.data?.registerTenant?.success).to.equal(true);
        });

        test("Tenant returned", function() {
          expect(body?.data?.registerTenant?.tenant).to.exist;
        });

        test("Tenant has an ID", function() {
          expect(body?.data?.registerTenant?.tenant?.id).to.exist;
        });

        test("Tenant name matches input", function() {
          expect(body?.data?.registerTenant?.tenant?.name).to.equal(vars.input.name);
        });

settings:
  encodeUrl: true
  timeout: 5000
  followRedirects: true
  maxRedirects: 5
