# Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen. DO NOT EDIT.
# Source: backend/management/api/testdata/examples/serviceroles.example.yaml

info:
  #name: Get Service Roles - List assignable service roles
  name: Get Service Roles
  type: graphql
  seq: 2

docs: |-
  Returns the fixed set of per-service gate roles that can be assigned to users (inventory_service, picking_service, …). Tenant-admin only.


graphql:
  method: POST
  url: "{{env_baseurl_management_query}}"
  auth: inherit
  body:
    query: |-
      # Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen. DO NOT EDIT.
      # Source: backend/management/api/testdata/examples/serviceroles.example.yaml

      query GetServiceRoles {
        serviceRoles {
          key
        }
      }

runtime:
  scripts:
    - type: tests
      code: |-
        // Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen. DO NOT EDIT.
        // Source: backend/management/api/testdata/examples/serviceroles.example.yaml

        var body = res.body;

        test("Status is 200", function() {
          expect(res.status).to.equal(200);
        });

        test("No GraphQL errors", function() {
          expect(body?.errors).to.not.exist;
        });

        test("Returns the service-role catalog", function() {
          expect(body?.data?.serviceRoles).to.not.be.empty;
          body?.data?.serviceRoles?.forEach(function(item) {
            expect(item?.key).to.exist;
          });
        });

settings:
  encodeUrl: true
  timeout: 5000
  followRedirects: true
  maxRedirects: 5
