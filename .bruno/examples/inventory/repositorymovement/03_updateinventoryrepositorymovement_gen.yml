# Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen. DO NOT EDIT.
# Source: backend/inventory/api/testdata/examples/updateinventoryrepositorymovement.example.yaml

info:
  #name: Update Inventory Repository Movement - Update Inventory Repository Movement
  name: Update Inventory Repository Movement
  type: graphql
  seq: 3

docs: |-
  Updates a pending inventory repository movement. Only non-immutable fields (handler, blockedBy) can be changed after creation. Executed movements cannot be updated or deleted.


graphql:
  method: POST
  url: "{{env_baseurl_inventory_query}}"
  auth: inherit
  body:
    query: |-
      # Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen. DO NOT EDIT.
      # Source: backend/inventory/api/testdata/examples/updateinventoryrepositorymovement.example.yaml
      #
      # ┌──────────────────────────────────────────────────────────┐
      # │  (!) ACTION REQUIRED                                     │
      # │                                                          │
      # │  Open the Variables tab and replace every value          │
      # │  marked with // PLACEHOLDER before running this request. │
      # └──────────────────────────────────────────────────────────┘

      mutation UpdateInventoryRepositoryMovement($id: ID!, $input: UpdateRepositoryMovementInput!) {
        updateInventoryRepositoryMovement(id: $id, input: $input) {
          inventoryRepositoryMovement {
            blockedBy
            collectionID
            createdAt
            createdBy
            data
            dataTypeID
            dataTypeSlug
            deletedAt
            deletedBy
            executed
            executedAt
            fromID
            handler
            id
            orderID
            position
            repositoryID
            tenantID
            toID
            updatedAt
            updatedBy
          }
          workflows {
            id
            runID
            type
          }
        }
      }
    variables: |-
      // Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen.
      // Source: backend/inventory/api/testdata/examples/updateinventoryrepositorymovement.example.yaml
      //
      // ┌──────────────────────────────────────────────────────────┐
      // │  (!) ACTION REQUIRED                                     │
      // │                                                          │
      // │  Replace every value marked with // PLACEHOLDER before   │
      // │  running this request.                                   │
      // └──────────────────────────────────────────────────────────┘

      {
        "id": null, // PLACEHOLDER: repository movement ID
        "input": {
          "blockedBy": null, // PLACEHOLDER: movement ID that blocks this one (optional)
          "handler": "operator-updated"
        }
      }

runtime:
  scripts:
    - type: tests
      code: |-
        // Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen. DO NOT EDIT.
        // Source: backend/inventory/api/testdata/examples/updateinventoryrepositorymovement.example.yaml

        var body = res.body;

        test("Status is 200", function() {
          expect(res.status).to.equal(200);
        });

        test("No GraphQL errors", function() {
          expect(body?.errors).to.not.exist;
        });

        test("Repository movement updated", function() {
          expect(body?.data?.updateInventoryRepositoryMovement?.inventoryRepositoryMovement).to.exist;
          expect(body?.data?.updateInventoryRepositoryMovement?.inventoryRepositoryMovement?.id).to.exist;
          expect(body?.data?.updateInventoryRepositoryMovement?.inventoryRepositoryMovement?.handler).to.equal('operator-updated');
        });

settings:
  encodeUrl: true
  timeout: 5000
  followRedirects: true
  maxRedirects: 5
