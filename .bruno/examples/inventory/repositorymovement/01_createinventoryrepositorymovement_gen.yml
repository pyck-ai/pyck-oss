# Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen. DO NOT EDIT.
# Source: backend/inventory/api/testdata/examples/createinventoryrepositorymovement.example.yaml

info:
  #name: Create Inventory Repository Movement - Create Inventory Repository Movement
  name: Create Inventory Repository Movement
  type: graphql
  seq: 1

docs: |-
  Creates a repository movement record to transfer all items from one repository to another. The movement is not executed until ExecuteInventoryRepositoryMovement is called.


graphql:
  method: POST
  url: "{{env_baseurl_inventory_query}}"
  auth: inherit
  body:
    query: |-
      # Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen. DO NOT EDIT.
      # Source: backend/inventory/api/testdata/examples/createinventoryrepositorymovement.example.yaml
      #
      # ┌──────────────────────────────────────────────────────────┐
      # │  (!) ACTION REQUIRED                                     │
      # │                                                          │
      # │  Open the Variables tab and replace every value          │
      # │  marked with // PLACEHOLDER before running this request. │
      # └──────────────────────────────────────────────────────────┘

      mutation CreateInventoryRepositoryMovement($input: CreateRepositoryMovementInput!) {
        createInventoryRepositoryMovement(input: $input) {
          inventoryRepositoryMovement {
            blockedBy
            collectionID
            createdAt
            createdBy
            data
            dataTypeID
            dataTypeSlug
            deletedAt
            deletedBy
            executed
            executedAt
            fromID
            handler
            id
            orderID
            position
            repositoryID
            tenantID
            toID
            updatedAt
            updatedBy
          }
          workflows {
            id
            runID
            type
          }
        }
      }
    variables: |-
      // Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen.
      // Source: backend/inventory/api/testdata/examples/createinventoryrepositorymovement.example.yaml
      //
      // ┌──────────────────────────────────────────────────────────┐
      // │  (!) ACTION REQUIRED                                     │
      // │                                                          │
      // │  Replace every value marked with // PLACEHOLDER before   │
      // │  running this request.                                   │
      // └──────────────────────────────────────────────────────────┘

      {
        "input": {
          "handler": "operator-01",
          "repositoryID": null, // PLACEHOLDER: repository ID to move
          "toID": null // PLACEHOLDER: destination repository ID
        }
      }

runtime:
  scripts:
    - type: tests
      code: |-
        // Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen. DO NOT EDIT.
        // Source: backend/inventory/api/testdata/examples/createinventoryrepositorymovement.example.yaml

        var body = res.body;

        test("Status is 200", function() {
          expect(res.status).to.equal(200);
        });

        test("No GraphQL errors", function() {
          expect(body?.errors).to.not.exist;
        });

        test("Repository movement created", function() {
          expect(body?.data?.createInventoryRepositoryMovement?.inventoryRepositoryMovement).to.exist;
          expect(body?.data?.createInventoryRepositoryMovement?.inventoryRepositoryMovement?.id).to.exist;
          expect(body?.data?.createInventoryRepositoryMovement?.inventoryRepositoryMovement?.executed).to.equal(false);
        });

settings:
  encodeUrl: true
  timeout: 5000
  followRedirects: true
  maxRedirects: 5
