# Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen. DO NOT EDIT.
# Source: backend/inventory/api/testdata/examples/createinventoryitemset.example.yaml

info:
  #name: Create Inventory Item Set - Create Inventory Item Set
  name: Create Inventory Item Set
  type: graphql
  seq: 1

docs: |-
  Creates a new inventory item set (a named grouping of inventory items sharing a SKU).


graphql:
  method: POST
  url: "{{env_baseurl_inventory_query}}"
  auth: inherit
  body:
    query: |-
      # Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen. DO NOT EDIT.
      # Source: backend/inventory/api/testdata/examples/createinventoryitemset.example.yaml
      #
      # ┌──────────────────────────────────────────────────────────┐
      # │  (!) ACTION REQUIRED                                     │
      # │                                                          │
      # │  Open the Variables tab and replace every value          │
      # │  marked with // PLACEHOLDER before running this request. │
      # └──────────────────────────────────────────────────────────┘

      mutation CreateInventoryItemSet($input: CreateInventoryItemSetInput!) {
        createInventoryItemSet(input: $input) {
          inventoryItemSet {
            createdAt
            createdBy
            data
            dataTypeID
            dataTypeSlug
            deletedAt
            deletedBy
            id
            items {
              edges {
                cursor
              }
              pageInfo {
                endCursor
                hasNextPage
                hasPreviousPage
                startCursor
              }
              totalCount
            }
            sku
            tenantID
            updatedAt
            updatedBy
          }
          workflows {
            id
            runID
            type
          }
        }
      }
    variables: |-
      // Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen.
      // Source: backend/inventory/api/testdata/examples/createinventoryitemset.example.yaml
      //
      // ┌──────────────────────────────────────────────────────────┐
      // │  (!) ACTION REQUIRED                                     │
      // │                                                          │
      // │  Replace every value marked with // PLACEHOLDER before   │
      // │  running this request.                                   │
      // └──────────────────────────────────────────────────────────┘

      {
        "input": {
          "itemIDs": [
            null, // PLACEHOLDER: inventory item ID
            null, // PLACEHOLDER: inventory item ID
            null // PLACEHOLDER: inventory item ID
          ],
          "sku": "WIDGET-SET-001"
        }
      }

runtime:
  scripts:
    - type: tests
      code: |-
        // Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen. DO NOT EDIT.
        // Source: backend/inventory/api/testdata/examples/createinventoryitemset.example.yaml

        var body = res.body;
        var vars = req.body.variables;

        test("Status is 200", function() {
          expect(res.status).to.equal(200);
        });

        test("No GraphQL errors", function() {
          expect(body?.errors).to.not.exist;
        });

        test("Item set created", function() {
          expect(body?.data?.createInventoryItemSet?.inventoryItemSet).to.exist;
          expect(body?.data?.createInventoryItemSet?.inventoryItemSet?.id).to.exist;
          expect(body?.data?.createInventoryItemSet?.inventoryItemSet?.sku).to.equal(vars.input.sku);
        });

settings:
  encodeUrl: true
  timeout: 5000
  followRedirects: true
  maxRedirects: 5
