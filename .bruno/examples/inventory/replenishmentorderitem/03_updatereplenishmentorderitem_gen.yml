# Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen. DO NOT EDIT.
# Source: backend/inventory/api/testdata/examples/updatereplenishmentorderitem.example.yaml

info:
  #name: Update Replenishment Order Item - Update Replenishment Order Item
  name: Update Replenishment Order Item
  type: graphql
  seq: 3

docs: |-
  Updates the quantity or SKU of an existing replenishment order item.


graphql:
  method: POST
  url: "{{env_baseurl_inventory_query}}"
  auth: inherit
  body:
    query: |-
      # Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen. DO NOT EDIT.
      # Source: backend/inventory/api/testdata/examples/updatereplenishmentorderitem.example.yaml
      #
      # ┌──────────────────────────────────────────────────────────┐
      # │  (!) ACTION REQUIRED                                     │
      # │                                                          │
      # │  Open the Variables tab and replace every value          │
      # │  marked with // PLACEHOLDER before running this request. │
      # └──────────────────────────────────────────────────────────┘

      mutation UpdateReplenishmentOrderItem($id: ID!, $input: UpdateReplenishmentOrderItemInput!) {
        updateReplenishmentOrderItem(id: $id, input: $input) {
          replenishmentOrderItem {
            createdAt
            createdBy
            data
            dataTypeID
            dataTypeSlug
            deletedAt
            deletedBy
            id
            quantity
            replenishmentOrderID
            sku
            tenantID
            updatedAt
            updatedBy
          }
          workflows {
            id
            runID
            type
          }
        }
      }
    variables: |-
      // Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen.
      // Source: backend/inventory/api/testdata/examples/updatereplenishmentorderitem.example.yaml
      //
      // ┌──────────────────────────────────────────────────────────┐
      // │  (!) ACTION REQUIRED                                     │
      // │                                                          │
      // │  Replace every value marked with // PLACEHOLDER before   │
      // │  running this request.                                   │
      // └──────────────────────────────────────────────────────────┘

      {
        "id": null, // PLACEHOLDER: replenishment order item ID
        "input": {
          "quantity": 75
        }
      }

runtime:
  scripts:
    - type: tests
      code: |-
        // Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen. DO NOT EDIT.
        // Source: backend/inventory/api/testdata/examples/updatereplenishmentorderitem.example.yaml

        var body = res.body;

        test("Status is 200", function() {
          expect(res.status).to.equal(200);
        });

        test("No GraphQL errors", function() {
          expect(body?.errors).to.not.exist;
        });

        test("Replenishment order item updated", function() {
          expect(body?.data?.updateReplenishmentOrderItem?.replenishmentOrderItem).to.exist;
          expect(body?.data?.updateReplenishmentOrderItem?.replenishmentOrderItem?.id).to.exist;
          expect(body?.data?.updateReplenishmentOrderItem?.replenishmentOrderItem?.quantity).to.equal(75);
        });

settings:
  encodeUrl: true
  timeout: 5000
  followRedirects: true
  maxRedirects: 5
