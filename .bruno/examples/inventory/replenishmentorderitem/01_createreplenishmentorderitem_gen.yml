# Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen. DO NOT EDIT.
# Source: backend/inventory/api/testdata/examples/createreplenishmentorderitem.example.yaml

info:
  #name: Create Replenishment Order Item - Create Replenishment Order Item
  name: Create Replenishment Order Item
  type: graphql
  seq: 1

docs: |-
  Adds an individual item to an existing replenishment order.


graphql:
  method: POST
  url: "{{env_baseurl_inventory_query}}"
  auth: inherit
  body:
    query: |-
      # Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen. DO NOT EDIT.
      # Source: backend/inventory/api/testdata/examples/createreplenishmentorderitem.example.yaml
      #
      # ┌──────────────────────────────────────────────────────────┐
      # │  (!) ACTION REQUIRED                                     │
      # │                                                          │
      # │  Open the Variables tab and replace every value          │
      # │  marked with // PLACEHOLDER before running this request. │
      # └──────────────────────────────────────────────────────────┘

      mutation CreateReplenishmentOrderItem($input: CreateReplenishmentOrderItemInput!) {
        createReplenishmentOrderItem(input: $input) {
          replenishmentOrderItem {
            createdAt
            createdBy
            data
            dataTypeID
            dataTypeSlug
            deletedAt
            deletedBy
            id
            quantity
            replenishmentOrderID
            sku
            tenantID
            updatedAt
            updatedBy
          }
          workflows {
            id
            runID
            type
          }
        }
      }
    variables: |-
      // Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen.
      // Source: backend/inventory/api/testdata/examples/createreplenishmentorderitem.example.yaml
      //
      // ┌──────────────────────────────────────────────────────────┐
      // │  (!) ACTION REQUIRED                                     │
      // │                                                          │
      // │  Replace every value marked with // PLACEHOLDER before   │
      // │  running this request.                                   │
      // └──────────────────────────────────────────────────────────┘

      {
        "input": {
          "quantity": 50,
          "replenishmentorderID": null, // PLACEHOLDER: replenishment order ID
          "sku": "RESTOCK-SKU-002"
        }
      }

runtime:
  scripts:
    - type: tests
      code: |-
        // Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen. DO NOT EDIT.
        // Source: backend/inventory/api/testdata/examples/createreplenishmentorderitem.example.yaml

        var body = res.body;
        var vars = req.body.variables;

        test("Status is 200", function() {
          expect(res.status).to.equal(200);
        });

        test("No GraphQL errors", function() {
          expect(body?.errors).to.not.exist;
        });

        test("Replenishment order item created", function() {
          expect(body?.data?.createReplenishmentOrderItem?.replenishmentOrderItem).to.exist;
          expect(body?.data?.createReplenishmentOrderItem?.replenishmentOrderItem?.id).to.exist;
          expect(body?.data?.createReplenishmentOrderItem?.replenishmentOrderItem?.sku).to.equal(vars.input.sku);
        });

settings:
  encodeUrl: true
  timeout: 5000
  followRedirects: true
  maxRedirects: 5
