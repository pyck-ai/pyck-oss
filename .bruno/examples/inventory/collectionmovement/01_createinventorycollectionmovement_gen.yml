# Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen. DO NOT EDIT.
# Source: backend/inventory/api/testdata/examples/createinventorycollectionmovement.example.yaml

info:
  #name: Create Inventory Collection Movement - Create Inventory Collection Movement
  name: Create Inventory Collection Movement
  type: graphql
  seq: 1

docs: |-
  Creates a collection of item movements in a single operation. The collection can mix item movements (using itemID) and repository movements (using repositoryID). Each entry specifies a quantity, source, and destination repository.


graphql:
  method: POST
  url: "{{env_baseurl_inventory_query}}"
  auth: inherit
  body:
    query: |-
      # Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen. DO NOT EDIT.
      # Source: backend/inventory/api/testdata/examples/createinventorycollectionmovement.example.yaml
      #
      # ┌──────────────────────────────────────────────────────────┐
      # │  (!) ACTION REQUIRED                                     │
      # │                                                          │
      # │  Open the Variables tab and replace every value          │
      # │  marked with // PLACEHOLDER before running this request. │
      # └──────────────────────────────────────────────────────────┘

      mutation CreateInventoryCollectionMovement($input: CreateCollectionMovementInput!) {
        createInventoryCollectionMovement(input: $input) {
          id
          movements {
            id
            movementType
          }
          workflows {
            id
            runID
            type
          }
        }
      }
    variables: |-
      // Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen.
      // Source: backend/inventory/api/testdata/examples/createinventorycollectionmovement.example.yaml
      //
      // ┌──────────────────────────────────────────────────────────┐
      // │  (!) ACTION REQUIRED                                     │
      // │                                                          │
      // │  Replace every value marked with // PLACEHOLDER before   │
      // │  running this request.                                   │
      // └──────────────────────────────────────────────────────────┘

      {
        "input": {
          "collection": [
            {
              "fromID": null, // PLACEHOLDER: source repository ID
              "handler": "operator-01",
              "itemID": null, // PLACEHOLDER: inventory item ID
              "quantity": 10,
              "toID": null // PLACEHOLDER: destination repository ID
            },
            {
              "fromID": null, // PLACEHOLDER: source repository ID (optional, inferred from repositoryID if omitted)
              "handler": "operator-01",
              "repositoryID": null, // PLACEHOLDER: repository ID to move
              "toID": null // PLACEHOLDER: destination repository ID
            }
          ],
          "handler": "operator-01"
        }
      }

runtime:
  scripts:
    - type: tests
      code: |-
        // Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen. DO NOT EDIT.
        // Source: backend/inventory/api/testdata/examples/createinventorycollectionmovement.example.yaml

        var body = res.body;

        test("Status is 200", function() {
          expect(res.status).to.equal(200);
        });

        test("No GraphQL errors", function() {
          expect(body?.errors).to.not.exist;
        });

        test("Collection movement created", function() {
          expect(body?.data?.createInventoryCollectionMovement?.id).to.exist;
        });

settings:
  encodeUrl: true
  timeout: 5000
  followRedirects: true
  maxRedirects: 5
