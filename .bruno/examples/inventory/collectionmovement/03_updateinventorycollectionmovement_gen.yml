# Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen. DO NOT EDIT.
# Source: backend/inventory/api/testdata/examples/updateinventorycollectionmovement.example.yaml

info:
  #name: Update Inventory Collection Movement - Update Inventory Collection Movement
  name: Update Inventory Collection Movement
  type: graphql
  seq: 3

docs: |-
  Updates an existing inventory collection movement. Only the handler field can be changed after creation.


graphql:
  method: POST
  url: "{{env_baseurl_inventory_query}}"
  auth: inherit
  body:
    query: |-
      # Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen. DO NOT EDIT.
      # Source: backend/inventory/api/testdata/examples/updateinventorycollectionmovement.example.yaml
      #
      # ┌──────────────────────────────────────────────────────────┐
      # │  (!) ACTION REQUIRED                                     │
      # │                                                          │
      # │  Open the Variables tab and replace every value          │
      # │  marked with // PLACEHOLDER before running this request. │
      # └──────────────────────────────────────────────────────────┘

      mutation UpdateInventoryCollectionMovement($id: ID!, $input: UpdateInventoryCollectionInput!) {
        updateInventoryCollectionMovement(id: $id, input: $input) {
          inventoryCollection {
            createdAt
            createdBy
            data
            dataTypeID
            dataTypeSlug
            deletedAt
            deletedBy
            handler
            id
            tenantID
            updatedAt
            updatedBy
          }
          workflows {
            id
            runID
            type
          }
        }
      }
    variables: |-
      // Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen.
      // Source: backend/inventory/api/testdata/examples/updateinventorycollectionmovement.example.yaml
      //
      // ┌──────────────────────────────────────────────────────────┐
      // │  (!) ACTION REQUIRED                                     │
      // │                                                          │
      // │  Replace every value marked with // PLACEHOLDER before   │
      // │  running this request.                                   │
      // └──────────────────────────────────────────────────────────┘

      {
        "id": null, // PLACEHOLDER: collection movement ID
        "input": {
          "handler": "handler-updated-01"
        }
      }

runtime:
  scripts:
    - type: tests
      code: |-
        // Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen. DO NOT EDIT.
        // Source: backend/inventory/api/testdata/examples/updateinventorycollectionmovement.example.yaml

        var body = res.body;

        test("Status is 200", function() {
          expect(res.status).to.equal(200);
        });

        test("No GraphQL errors", function() {
          expect(body?.errors).to.not.exist;
        });

        test("Collection movement updated", function() {
          expect(body?.data?.updateInventoryCollectionMovement?.inventoryCollection).to.exist;
          expect(body?.data?.updateInventoryCollectionMovement?.inventoryCollection?.handler).to.equal('handler-updated-01');
        });

settings:
  encodeUrl: true
  timeout: 5000
  followRedirects: true
  maxRedirects: 5
