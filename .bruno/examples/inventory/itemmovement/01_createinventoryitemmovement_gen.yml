# Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen. DO NOT EDIT.
# Source: backend/inventory/api/testdata/examples/createinventoryitemmovement.example.yaml

info:
  #name: Create Inventory Item Movement - Create Inventory Item Movement
  name: Create Inventory Item Movement
  type: graphql
  seq: 1

docs: |-
  Creates an item movement record to transfer a quantity of an inventory item from one repository to another. The movement is not executed until ExecuteInventoryItemMovement is called.


graphql:
  method: POST
  url: "{{env_baseurl_inventory_query}}"
  auth: inherit
  body:
    query: |-
      # Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen. DO NOT EDIT.
      # Source: backend/inventory/api/testdata/examples/createinventoryitemmovement.example.yaml
      #
      # ┌──────────────────────────────────────────────────────────┐
      # │  (!) ACTION REQUIRED                                     │
      # │                                                          │
      # │  Open the Variables tab and replace every value          │
      # │  marked with // PLACEHOLDER before running this request. │
      # └──────────────────────────────────────────────────────────┘

      mutation CreateInventoryItemMovement($input: CreateItemMovementInput!) {
        createInventoryItemMovement(input: $input) {
          inventoryItemMovement {
            blockedBy
            collectionID
            createdAt
            createdBy
            data
            dataTypeID
            dataTypeSlug
            deletedAt
            deletedBy
            executed
            executedAt
            fromID
            handler
            id
            itemID
            orderID
            position
            quantity
            tenantID
            toID
            updatedAt
            updatedBy
          }
          workflows {
            id
            runID
            type
          }
        }
      }
    variables: |-
      // Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen.
      // Source: backend/inventory/api/testdata/examples/createinventoryitemmovement.example.yaml
      //
      // ┌──────────────────────────────────────────────────────────┐
      // │  (!) ACTION REQUIRED                                     │
      // │                                                          │
      // │  Replace every value marked with // PLACEHOLDER before   │
      // │  running this request.                                   │
      // └──────────────────────────────────────────────────────────┘

      {
        "input": {
          "fromID": null, // PLACEHOLDER: source repository ID
          "handler": "operator-01",
          "itemID": null, // PLACEHOLDER: inventory item ID
          "quantity": 10,
          "toID": null // PLACEHOLDER: destination repository ID
        }
      }

runtime:
  scripts:
    - type: tests
      code: |-
        // Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen. DO NOT EDIT.
        // Source: backend/inventory/api/testdata/examples/createinventoryitemmovement.example.yaml

        var body = res.body;

        test("Status is 200", function() {
          expect(res.status).to.equal(200);
        });

        test("No GraphQL errors", function() {
          expect(body?.errors).to.not.exist;
        });

        test("Item movement created", function() {
          expect(body?.data?.createInventoryItemMovement?.inventoryItemMovement).to.exist;
          expect(body?.data?.createInventoryItemMovement?.inventoryItemMovement?.id).to.exist;
          expect(body?.data?.createInventoryItemMovement?.inventoryItemMovement?.executed).to.equal(false);
        });

settings:
  encodeUrl: true
  timeout: 5000
  followRedirects: true
  maxRedirects: 5
