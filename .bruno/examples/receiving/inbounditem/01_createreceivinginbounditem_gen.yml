# Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen. DO NOT EDIT.
# Source: backend/receiving/api/testdata/examples/createreceivinginbounditem.example.yaml

info:
  #name: Create Receiving Inbound Item - Create Receiving Inbound Item
  name: Create Receiving Inbound Item
  type: graphql
  seq: 1

docs: |-
  Creates a single inbound item associated with an existing inbound.


graphql:
  method: POST
  url: "{{env_baseurl_receiving_query}}"
  auth: inherit
  body:
    query: |-
      # Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen. DO NOT EDIT.
      # Source: backend/receiving/api/testdata/examples/createreceivinginbounditem.example.yaml
      #
      # ┌──────────────────────────────────────────────────────────┐
      # │  (!) ACTION REQUIRED                                     │
      # │                                                          │
      # │  Open the Variables tab and replace every value          │
      # │  marked with // PLACEHOLDER before running this request. │
      # └──────────────────────────────────────────────────────────┘

      mutation CreateReceivingInboundItem($input: CreateReceivingInboundItemInput!) {
        createReceivingInboundItem(input: $input) {
          receivingInboundItem {
            createdAt
            createdBy
            data
            dataTypeID
            dataTypeSlug
            deletedAt
            deletedBy
            id
            inboundID
            quantity
            sku
            tenantID
            updatedAt
            updatedBy
          }
          workflows {
            id
            runID
            type
          }
        }
      }
    variables: |-
      // Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen.
      // Source: backend/receiving/api/testdata/examples/createreceivinginbounditem.example.yaml
      //
      // ┌──────────────────────────────────────────────────────────┐
      // │  (!) ACTION REQUIRED                                     │
      // │                                                          │
      // │  Replace every value marked with // PLACEHOLDER before   │
      // │  running this request.                                   │
      // └──────────────────────────────────────────────────────────┘

      {
        "input": {
          "inboundID": null, // PLACEHOLDER: inbound ID
          "quantity": 100,
          "sku": "RECV-ITEM-001"
        }
      }

runtime:
  scripts:
    - type: tests
      code: |-
        // Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen. DO NOT EDIT.
        // Source: backend/receiving/api/testdata/examples/createreceivinginbounditem.example.yaml

        var body = res.body;

        test("Status is 200", function() {
          expect(res.status).to.equal(200);
        });

        test("No GraphQL errors", function() {
          expect(body?.errors).to.not.exist;
        });

        test("Item was created", function() {
          expect(body?.data?.createReceivingInboundItem?.receivingInboundItem?.id).to.exist;
          expect(body?.data?.createReceivingInboundItem?.receivingInboundItem?.sku).to.equal('RECV-ITEM-001');
          expect(body?.data?.createReceivingInboundItem?.receivingInboundItem?.quantity).to.equal(100);
        });

settings:
  encodeUrl: true
  timeout: 5000
  followRedirects: true
  maxRedirects: 5
