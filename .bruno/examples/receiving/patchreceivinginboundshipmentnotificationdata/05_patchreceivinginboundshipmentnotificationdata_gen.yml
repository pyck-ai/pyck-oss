# Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen. DO NOT EDIT.
# Source: backend/receiving/api/testdata/examples/patchreceivinginboundshipmentnotificationdata.example.yaml

info:
  #name: Patch Receiving Inbound Shipment Notification Data - Patch Receiving Inbound Shipment Notification Data
  name: Patch Receiving Inbound Shipment Notification Data
  type: graphql
  seq: 5

docs: |-
  Applies RFC 6902 JSON Patch operations to the receiving inbound shipment notification's data field. Only changed fields are modified; untouched fields are preserved. See https://datatracker.ietf.org/doc/html/rfc6902 for the specification.


graphql:
  method: POST
  url: "{{env_baseurl_receiving_query}}"
  auth: inherit
  body:
    query: |-
      # Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen. DO NOT EDIT.
      # Source: backend/receiving/api/testdata/examples/patchreceivinginboundshipmentnotificationdata.example.yaml
      #
      # ┌──────────────────────────────────────────────────────────┐
      # │  (!) ACTION REQUIRED                                     │
      # │                                                          │
      # │  Open the Variables tab and replace every value          │
      # │  marked with // PLACEHOLDER before running this request. │
      # └──────────────────────────────────────────────────────────┘

      mutation PatchReceivingInboundShipmentNotificationData($id: ID!, $patches: [JSONPatchInput!]!) {
        patchReceivingInboundShipmentNotificationData(id: $id, patches: $patches) {
          receivingInboundShipmentNotification {
            createdAt
            createdBy
            data
            dataTypeID
            dataTypeSlug
            deletedAt
            deletedBy
            id
            inboundID
            tenantID
            updatedAt
            updatedBy
          }
          workflows {
            id
            runID
            type
          }
        }
      }
    variables: |-
      // Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen.
      // Source: backend/receiving/api/testdata/examples/patchreceivinginboundshipmentnotificationdata.example.yaml
      //
      // ┌──────────────────────────────────────────────────────────┐
      // │  (!) ACTION REQUIRED                                     │
      // │                                                          │
      // │  Replace every value marked with // PLACEHOLDER before   │
      // │  running this request.                                   │
      // └──────────────────────────────────────────────────────────┘

      {
        "id": null, // PLACEHOLDER: receiving inbound shipment notification id to patch
        "patches": [
          {
            "op": "REPLACE",
            "path": "/meta/name",
            "value": "\"PatchedNotification\""
          },
          {
            "op": "REPLACE",
            "path": "/sum",
            "value": "42"
          }
        ]
      }

runtime:
  scripts:
    - type: tests
      code: |-
        // Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen. DO NOT EDIT.
        // Source: backend/receiving/api/testdata/examples/patchreceivinginboundshipmentnotificationdata.example.yaml

        var body = res.body;
        var vars = req.body.variables;

        test("Status is 200", function() {
          expect(res.status).to.equal(200);
        });

        test("No GraphQL errors", function() {
          expect(body?.errors).to.not.exist;
        });

        test("Patched notification returned", function() {
          expect(body?.data?.patchReceivingInboundShipmentNotificationData?.receivingInboundShipmentNotification).to.exist;
          expect(body?.data?.patchReceivingInboundShipmentNotificationData?.receivingInboundShipmentNotification?.id).to.equal(vars.id);
        });

settings:
  encodeUrl: true
  timeout: 5000
  followRedirects: true
  maxRedirects: 5
