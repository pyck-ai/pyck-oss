# Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen. DO NOT EDIT.
# Source: backend/receiving/api/testdata/examples/createreceivinginbound.example.yaml

info:
  #name: Create Receiving Inbound - Create Receiving Inbound
  name: Create Receiving Inbound
  type: graphql
  seq: 1

docs: |-
  Creates a receiving inbound with supplier, order, data, and inline items.


graphql:
  method: POST
  url: "{{env_baseurl_receiving_query}}"
  auth: inherit
  body:
    query: |-
      # Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen. DO NOT EDIT.
      # Source: backend/receiving/api/testdata/examples/createreceivinginbound.example.yaml
      #
      # ┌──────────────────────────────────────────────────────────┐
      # │  (!) ACTION REQUIRED                                     │
      # │                                                          │
      # │  Open the Variables tab and replace every value          │
      # │  marked with // PLACEHOLDER before running this request. │
      # └──────────────────────────────────────────────────────────┘

      mutation CreateReceivingInbound($input: CreateReceivingInboundWithItemsInput!) {
        createReceivingInbound(input: $input) {
          receivingInbound {
            createdAt
            createdBy
            data
            dataTypeID
            dataTypeSlug
            deletedAt
            deletedBy
            id
            orderID
            supplierID
            tenantID
            updatedAt
            updatedBy
          }
          workflows {
            id
            runID
            type
          }
        }
      }
    variables: |-
      // Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen.
      // Source: backend/receiving/api/testdata/examples/createreceivinginbound.example.yaml
      //
      // ┌──────────────────────────────────────────────────────────┐
      // │  (!) ACTION REQUIRED                                     │
      // │                                                          │
      // │  Replace every value marked with // PLACEHOLDER before   │
      // │  running this request.                                   │
      // └──────────────────────────────────────────────────────────┘

      {
        "input": {
          "data": {
            "receivedBy": "dock-3",
            "warehouse": "WH-A"
          },
          "inboundItems": [
            {
              "quantity": 50,
              "sku": "RECV-SKU-001"
            },
            {
              "quantity": 25,
              "sku": "RECV-SKU-002"
            }
          ],
          "orderID": "PO-2026-001",
          "supplierID": null // PLACEHOLDER: supplier ID
        }
      }

runtime:
  scripts:
    - type: tests
      code: |-
        // Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen. DO NOT EDIT.
        // Source: backend/receiving/api/testdata/examples/createreceivinginbound.example.yaml

        var body = res.body;

        test("Status is 200", function() {
          expect(res.status).to.equal(200);
        });

        test("No GraphQL errors", function() {
          expect(body?.errors).to.not.exist;
        });

        test("Inbound was created", function() {
          expect(body?.data?.createReceivingInbound?.receivingInbound?.id).to.exist;
          expect(body?.data?.createReceivingInbound?.receivingInbound?.orderID).to.equal('PO-2026-001');
          expect(body?.data?.createReceivingInbound?.receivingInbound?.inboundItems?.totalCount).to.equal(2);
        });

settings:
  encodeUrl: true
  timeout: 5000
  followRedirects: true
  maxRedirects: 5
