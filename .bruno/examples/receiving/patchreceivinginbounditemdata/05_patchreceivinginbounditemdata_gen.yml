# Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen. DO NOT EDIT.
# Source: backend/receiving/api/testdata/examples/patchreceivinginbounditemdata.example.yaml

info:
  #name: Patch Receiving Inbound Item Data - Patch Receiving Inbound Item Data
  name: Patch Receiving Inbound Item Data
  type: graphql
  seq: 5

docs: |-
  Applies RFC 6902 JSON Patch operations to the inbound item's data field. Only changed fields are modified; untouched fields are preserved.


graphql:
  method: POST
  url: "{{env_baseurl_receiving_query}}"
  auth: inherit
  body:
    query: |-
      # Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen. DO NOT EDIT.
      # Source: backend/receiving/api/testdata/examples/patchreceivinginbounditemdata.example.yaml
      #
      # ┌──────────────────────────────────────────────────────────┐
      # │  (!) ACTION REQUIRED                                     │
      # │                                                          │
      # │  Open the Variables tab and replace every value          │
      # │  marked with // PLACEHOLDER before running this request. │
      # └──────────────────────────────────────────────────────────┘

      mutation PatchReceivingInboundItemData($id: ID!, $patches: [JSONPatchInput!]!) {
        patchReceivingInboundItemData(id: $id, patches: $patches) {
          receivingInboundItem {
            createdAt
            createdBy
            data
            dataTypeID
            dataTypeSlug
            deletedAt
            deletedBy
            id
            inboundID
            quantity
            sku
            tenantID
            updatedAt
            updatedBy
          }
          workflows {
            id
            runID
            type
          }
        }
      }
    variables: |-
      // Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen.
      // Source: backend/receiving/api/testdata/examples/patchreceivinginbounditemdata.example.yaml
      //
      // ┌──────────────────────────────────────────────────────────┐
      // │  (!) ACTION REQUIRED                                     │
      // │                                                          │
      // │  Replace every value marked with // PLACEHOLDER before   │
      // │  running this request.                                   │
      // └──────────────────────────────────────────────────────────┘

      {
        "id": null, // PLACEHOLDER: inbound item id to patch
        "patches": [
          {
            "op": "REPLACE",
            "path": "/inspected",
            "value": "true"
          },
          {
            "op": "REPLACE",
            "path": "/condition",
            "value": "\"inspected-ok\""
          }
        ]
      }

runtime:
  scripts:
    - type: tests
      code: |-
        // Code generated by github.com/pyck-ai/pyck/backend/common/cmd/brunogen. DO NOT EDIT.
        // Source: backend/receiving/api/testdata/examples/patchreceivinginbounditemdata.example.yaml

        var body = res.body;
        var vars = req.body.variables;

        test("Status is 200", function() {
          expect(res.status).to.equal(200);
        });

        test("No GraphQL errors", function() {
          expect(body?.errors).to.not.exist;
        });

        test("Patched inbound item returned", function() {
          expect(body?.data?.patchReceivingInboundItemData?.receivingInboundItem).to.exist;
          expect(body?.data?.patchReceivingInboundItemData?.receivingInboundItem?.id).to.equal(vars.id);
        });

settings:
  encodeUrl: true
  timeout: 5000
  followRedirects: true
  maxRedirects: 5
