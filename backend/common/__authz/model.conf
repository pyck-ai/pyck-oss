[request_definition]
r = sub, dom, obj, act

[policy_definition]
p = sub, dom, obj, act, eft

[role_definition]
g = _, _, _

[policy_effect]
e = some(where (eft == allow))

[matchers]
m = g(r.sub, p.sub, r.dom) && r.dom == p.dom && wildcardMatch(p.obj, r.obj) && wildcardMatch(p.act, r.act)