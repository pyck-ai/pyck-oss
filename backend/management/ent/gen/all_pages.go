// Code generated by ent, DO NOT EDIT.

package gen

import "context"

// AllPages paginates through all AccessPolicy records matching the current
// query predicates in pages of pageSize, collecting every row. This avoids the
// LimitMixin hard cap that silently truncates results when no explicit Limit is
// set.
func (_q *AccessPolicyQuery) AllPages(ctx context.Context, pageSize int) ([]*AccessPolicy, error) {
	var all []*AccessPolicy
	for offset := 0; ; offset += pageSize {
		page, err := _q.Clone().Limit(pageSize).Offset(offset).All(ctx)
		if err != nil {
			return nil, err
		}
		all = append(all, page...)
		if len(page) < pageSize {
			break
		}
	}
	return all, nil
}

// AllPages paginates through all DataType records matching the current
// query predicates in pages of pageSize, collecting every row. This avoids the
// LimitMixin hard cap that silently truncates results when no explicit Limit is
// set.
func (_q *DataTypeQuery) AllPages(ctx context.Context, pageSize int) ([]*DataType, error) {
	var all []*DataType
	for offset := 0; ; offset += pageSize {
		page, err := _q.Clone().Limit(pageSize).Offset(offset).All(ctx)
		if err != nil {
			return nil, err
		}
		all = append(all, page...)
		if len(page) < pageSize {
			break
		}
	}
	return all, nil
}

// AllPages paginates through all Device records matching the current
// query predicates in pages of pageSize, collecting every row. This avoids the
// LimitMixin hard cap that silently truncates results when no explicit Limit is
// set.
func (_q *DeviceQuery) AllPages(ctx context.Context, pageSize int) ([]*Device, error) {
	var all []*Device
	for offset := 0; ; offset += pageSize {
		page, err := _q.Clone().Limit(pageSize).Offset(offset).All(ctx)
		if err != nil {
			return nil, err
		}
		all = append(all, page...)
		if len(page) < pageSize {
			break
		}
	}
	return all, nil
}

// AllPages paginates through all DeviceLocation records matching the current
// query predicates in pages of pageSize, collecting every row. This avoids the
// LimitMixin hard cap that silently truncates results when no explicit Limit is
// set.
func (_q *DeviceLocationQuery) AllPages(ctx context.Context, pageSize int) ([]*DeviceLocation, error) {
	var all []*DeviceLocation
	for offset := 0; ; offset += pageSize {
		page, err := _q.Clone().Limit(pageSize).Offset(offset).All(ctx)
		if err != nil {
			return nil, err
		}
		all = append(all, page...)
		if len(page) < pageSize {
			break
		}
	}
	return all, nil
}

// AllPages paginates through all DeviceUser records matching the current
// query predicates in pages of pageSize, collecting every row. This avoids the
// LimitMixin hard cap that silently truncates results when no explicit Limit is
// set.
func (_q *DeviceUserQuery) AllPages(ctx context.Context, pageSize int) ([]*DeviceUser, error) {
	var all []*DeviceUser
	for offset := 0; ; offset += pageSize {
		page, err := _q.Clone().Limit(pageSize).Offset(offset).All(ctx)
		if err != nil {
			return nil, err
		}
		all = append(all, page...)
		if len(page) < pageSize {
			break
		}
	}
	return all, nil
}

// AllPages paginates through all EntityEventsOutbox records matching the current
// query predicates in pages of pageSize, collecting every row. This avoids the
// LimitMixin hard cap that silently truncates results when no explicit Limit is
// set.
func (_q *EntityEventsOutboxQuery) AllPages(ctx context.Context, pageSize int) ([]*EntityEventsOutbox, error) {
	var all []*EntityEventsOutbox
	for offset := 0; ; offset += pageSize {
		page, err := _q.Clone().Limit(pageSize).Offset(offset).All(ctx)
		if err != nil {
			return nil, err
		}
		all = append(all, page...)
		if len(page) < pageSize {
			break
		}
	}
	return all, nil
}

// AllPages paginates through all Event records matching the current
// query predicates in pages of pageSize, collecting every row. This avoids the
// LimitMixin hard cap that silently truncates results when no explicit Limit is
// set.
func (_q *EventQuery) AllPages(ctx context.Context, pageSize int) ([]*Event, error) {
	var all []*Event
	for offset := 0; ; offset += pageSize {
		page, err := _q.Clone().Limit(pageSize).Offset(offset).All(ctx)
		if err != nil {
			return nil, err
		}
		all = append(all, page...)
		if len(page) < pageSize {
			break
		}
	}
	return all, nil
}

// AllPages paginates through all Group records matching the current
// query predicates in pages of pageSize, collecting every row. This avoids the
// LimitMixin hard cap that silently truncates results when no explicit Limit is
// set.
func (_q *GroupQuery) AllPages(ctx context.Context, pageSize int) ([]*Group, error) {
	var all []*Group
	for offset := 0; ; offset += pageSize {
		page, err := _q.Clone().Limit(pageSize).Offset(offset).All(ctx)
		if err != nil {
			return nil, err
		}
		all = append(all, page...)
		if len(page) < pageSize {
			break
		}
	}
	return all, nil
}

// AllPages paginates through all KeyValue records matching the current
// query predicates in pages of pageSize, collecting every row. This avoids the
// LimitMixin hard cap that silently truncates results when no explicit Limit is
// set.
func (_q *KeyValueQuery) AllPages(ctx context.Context, pageSize int) ([]*KeyValue, error) {
	var all []*KeyValue
	for offset := 0; ; offset += pageSize {
		page, err := _q.Clone().Limit(pageSize).Offset(offset).All(ctx)
		if err != nil {
			return nil, err
		}
		all = append(all, page...)
		if len(page) < pageSize {
			break
		}
	}
	return all, nil
}

// AllPages paginates through all Location records matching the current
// query predicates in pages of pageSize, collecting every row. This avoids the
// LimitMixin hard cap that silently truncates results when no explicit Limit is
// set.
func (_q *LocationQuery) AllPages(ctx context.Context, pageSize int) ([]*Location, error) {
	var all []*Location
	for offset := 0; ; offset += pageSize {
		page, err := _q.Clone().Limit(pageSize).Offset(offset).All(ctx)
		if err != nil {
			return nil, err
		}
		all = append(all, page...)
		if len(page) < pageSize {
			break
		}
	}
	return all, nil
}

// AllPages paginates through all Role records matching the current
// query predicates in pages of pageSize, collecting every row. This avoids the
// LimitMixin hard cap that silently truncates results when no explicit Limit is
// set.
func (_q *RoleQuery) AllPages(ctx context.Context, pageSize int) ([]*Role, error) {
	var all []*Role
	for offset := 0; ; offset += pageSize {
		page, err := _q.Clone().Limit(pageSize).Offset(offset).All(ctx)
		if err != nil {
			return nil, err
		}
		all = append(all, page...)
		if len(page) < pageSize {
			break
		}
	}
	return all, nil
}

// AllPages paginates through all Tenant records matching the current
// query predicates in pages of pageSize, collecting every row. This avoids the
// LimitMixin hard cap that silently truncates results when no explicit Limit is
// set.
func (_q *TenantQuery) AllPages(ctx context.Context, pageSize int) ([]*Tenant, error) {
	var all []*Tenant
	for offset := 0; ; offset += pageSize {
		page, err := _q.Clone().Limit(pageSize).Offset(offset).All(ctx)
		if err != nil {
			return nil, err
		}
		all = append(all, page...)
		if len(page) < pageSize {
			break
		}
	}
	return all, nil
}

// AllPages paginates through all User records matching the current
// query predicates in pages of pageSize, collecting every row. This avoids the
// LimitMixin hard cap that silently truncates results when no explicit Limit is
// set.
func (_q *UserQuery) AllPages(ctx context.Context, pageSize int) ([]*User, error) {
	var all []*User
	for offset := 0; ; offset += pageSize {
		page, err := _q.Clone().Limit(pageSize).Offset(offset).All(ctx)
		if err != nil {
			return nil, err
		}
		all = append(all, page...)
		if len(page) < pageSize {
			break
		}
	}
	return all, nil
}
