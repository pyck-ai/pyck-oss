// Code generated by ent, DO NOT EDIT.

package gen

import (
	"entgo.io/ent/dialect/sql"
	"github.com/pyck-ai/pyck/backend/management/ent/gen/predicate"
)

// DistinctOnExists filters AccessPolicy records to include only the latest per group
// of groupFields, ordered by orderField. Filters are propagated to the NOT EXISTS subquery.
func (_q *AccessPolicyQuery) DistinctOnExists(groupFields []string, orderField string, filters ...predicate.AccessPolicy) *AccessPolicyQuery {
	_q.Where(func(s *sql.Selector) {
		t := sql.Table("policies").As("s2")
		conditions := make([]*sql.Predicate, 0, len(groupFields)+1)
		for _, f := range groupFields {
			conditions = append(conditions, sql.ColumnsEQ(t.C(f), s.C(f)))
		}
		conditions = append(conditions, sql.ColumnsGT(t.C(orderField), s.C(orderField)))
		sub := sql.SelectExpr(sql.Expr("1")).From(t).Where(sql.And(conditions...))
		for _, pf := range filters {
			pf(sub)
		}
		s.Where(sql.Not(sql.Exists(sub)))
	})
	return _q
}

// DistinctOnExists filters DataType records to include only the latest per group
// of groupFields, ordered by orderField. Filters are propagated to the NOT EXISTS subquery.
func (_q *DataTypeQuery) DistinctOnExists(groupFields []string, orderField string, filters ...predicate.DataType) *DataTypeQuery {
	_q.Where(func(s *sql.Selector) {
		t := sql.Table("datatypes").As("s2")
		conditions := make([]*sql.Predicate, 0, len(groupFields)+1)
		for _, f := range groupFields {
			conditions = append(conditions, sql.ColumnsEQ(t.C(f), s.C(f)))
		}
		conditions = append(conditions, sql.ColumnsGT(t.C(orderField), s.C(orderField)))
		sub := sql.SelectExpr(sql.Expr("1")).From(t).Where(sql.And(conditions...))
		for _, pf := range filters {
			pf(sub)
		}
		s.Where(sql.Not(sql.Exists(sub)))
	})
	return _q
}

// DistinctOnExists filters Device records to include only the latest per group
// of groupFields, ordered by orderField. Filters are propagated to the NOT EXISTS subquery.
func (_q *DeviceQuery) DistinctOnExists(groupFields []string, orderField string, filters ...predicate.Device) *DeviceQuery {
	_q.Where(func(s *sql.Selector) {
		t := sql.Table("devices").As("s2")
		conditions := make([]*sql.Predicate, 0, len(groupFields)+1)
		for _, f := range groupFields {
			conditions = append(conditions, sql.ColumnsEQ(t.C(f), s.C(f)))
		}
		conditions = append(conditions, sql.ColumnsGT(t.C(orderField), s.C(orderField)))
		sub := sql.SelectExpr(sql.Expr("1")).From(t).Where(sql.And(conditions...))
		for _, pf := range filters {
			pf(sub)
		}
		s.Where(sql.Not(sql.Exists(sub)))
	})
	return _q
}

// DistinctOnExists filters DeviceLocation records to include only the latest per group
// of groupFields, ordered by orderField. Filters are propagated to the NOT EXISTS subquery.
func (_q *DeviceLocationQuery) DistinctOnExists(groupFields []string, orderField string, filters ...predicate.DeviceLocation) *DeviceLocationQuery {
	_q.Where(func(s *sql.Selector) {
		t := sql.Table("device_locations").As("s2")
		conditions := make([]*sql.Predicate, 0, len(groupFields)+1)
		for _, f := range groupFields {
			conditions = append(conditions, sql.ColumnsEQ(t.C(f), s.C(f)))
		}
		conditions = append(conditions, sql.ColumnsGT(t.C(orderField), s.C(orderField)))
		sub := sql.SelectExpr(sql.Expr("1")).From(t).Where(sql.And(conditions...))
		for _, pf := range filters {
			pf(sub)
		}
		s.Where(sql.Not(sql.Exists(sub)))
	})
	return _q
}

// DistinctOnExists filters DeviceUser records to include only the latest per group
// of groupFields, ordered by orderField. Filters are propagated to the NOT EXISTS subquery.
func (_q *DeviceUserQuery) DistinctOnExists(groupFields []string, orderField string, filters ...predicate.DeviceUser) *DeviceUserQuery {
	_q.Where(func(s *sql.Selector) {
		t := sql.Table("device_users").As("s2")
		conditions := make([]*sql.Predicate, 0, len(groupFields)+1)
		for _, f := range groupFields {
			conditions = append(conditions, sql.ColumnsEQ(t.C(f), s.C(f)))
		}
		conditions = append(conditions, sql.ColumnsGT(t.C(orderField), s.C(orderField)))
		sub := sql.SelectExpr(sql.Expr("1")).From(t).Where(sql.And(conditions...))
		for _, pf := range filters {
			pf(sub)
		}
		s.Where(sql.Not(sql.Exists(sub)))
	})
	return _q
}

// DistinctOnExists filters EntityEventsOutbox records to include only the latest per group
// of groupFields, ordered by orderField. Filters are propagated to the NOT EXISTS subquery.
func (_q *EntityEventsOutboxQuery) DistinctOnExists(groupFields []string, orderField string, filters ...predicate.EntityEventsOutbox) *EntityEventsOutboxQuery {
	_q.Where(func(s *sql.Selector) {
		t := sql.Table("event_outbox").As("s2")
		conditions := make([]*sql.Predicate, 0, len(groupFields)+1)
		for _, f := range groupFields {
			conditions = append(conditions, sql.ColumnsEQ(t.C(f), s.C(f)))
		}
		conditions = append(conditions, sql.ColumnsGT(t.C(orderField), s.C(orderField)))
		sub := sql.SelectExpr(sql.Expr("1")).From(t).Where(sql.And(conditions...))
		for _, pf := range filters {
			pf(sub)
		}
		s.Where(sql.Not(sql.Exists(sub)))
	})
	return _q
}

// DistinctOnExists filters Event records to include only the latest per group
// of groupFields, ordered by orderField. Filters are propagated to the NOT EXISTS subquery.
func (_q *EventQuery) DistinctOnExists(groupFields []string, orderField string, filters ...predicate.Event) *EventQuery {
	_q.Where(func(s *sql.Selector) {
		t := sql.Table("events").As("s2")
		conditions := make([]*sql.Predicate, 0, len(groupFields)+1)
		for _, f := range groupFields {
			conditions = append(conditions, sql.ColumnsEQ(t.C(f), s.C(f)))
		}
		conditions = append(conditions, sql.ColumnsGT(t.C(orderField), s.C(orderField)))
		sub := sql.SelectExpr(sql.Expr("1")).From(t).Where(sql.And(conditions...))
		for _, pf := range filters {
			pf(sub)
		}
		s.Where(sql.Not(sql.Exists(sub)))
	})
	return _q
}

// DistinctOnExists filters Group records to include only the latest per group
// of groupFields, ordered by orderField. Filters are propagated to the NOT EXISTS subquery.
func (_q *GroupQuery) DistinctOnExists(groupFields []string, orderField string, filters ...predicate.Group) *GroupQuery {
	_q.Where(func(s *sql.Selector) {
		t := sql.Table("groups").As("s2")
		conditions := make([]*sql.Predicate, 0, len(groupFields)+1)
		for _, f := range groupFields {
			conditions = append(conditions, sql.ColumnsEQ(t.C(f), s.C(f)))
		}
		conditions = append(conditions, sql.ColumnsGT(t.C(orderField), s.C(orderField)))
		sub := sql.SelectExpr(sql.Expr("1")).From(t).Where(sql.And(conditions...))
		for _, pf := range filters {
			pf(sub)
		}
		s.Where(sql.Not(sql.Exists(sub)))
	})
	return _q
}

// DistinctOnExists filters IdempotencyKey records to include only the latest per group
// of groupFields, ordered by orderField. Filters are propagated to the NOT EXISTS subquery.
func (_q *IdempotencyKeyQuery) DistinctOnExists(groupFields []string, orderField string, filters ...predicate.IdempotencyKey) *IdempotencyKeyQuery {
	_q.Where(func(s *sql.Selector) {
		t := sql.Table("idempotency_keys").As("s2")
		conditions := make([]*sql.Predicate, 0, len(groupFields)+1)
		for _, f := range groupFields {
			conditions = append(conditions, sql.ColumnsEQ(t.C(f), s.C(f)))
		}
		conditions = append(conditions, sql.ColumnsGT(t.C(orderField), s.C(orderField)))
		sub := sql.SelectExpr(sql.Expr("1")).From(t).Where(sql.And(conditions...))
		for _, pf := range filters {
			pf(sub)
		}
		s.Where(sql.Not(sql.Exists(sub)))
	})
	return _q
}

// DistinctOnExists filters KeyValue records to include only the latest per group
// of groupFields, ordered by orderField. Filters are propagated to the NOT EXISTS subquery.
func (_q *KeyValueQuery) DistinctOnExists(groupFields []string, orderField string, filters ...predicate.KeyValue) *KeyValueQuery {
	_q.Where(func(s *sql.Selector) {
		t := sql.Table("keyvalues").As("s2")
		conditions := make([]*sql.Predicate, 0, len(groupFields)+1)
		for _, f := range groupFields {
			conditions = append(conditions, sql.ColumnsEQ(t.C(f), s.C(f)))
		}
		conditions = append(conditions, sql.ColumnsGT(t.C(orderField), s.C(orderField)))
		sub := sql.SelectExpr(sql.Expr("1")).From(t).Where(sql.And(conditions...))
		for _, pf := range filters {
			pf(sub)
		}
		s.Where(sql.Not(sql.Exists(sub)))
	})
	return _q
}

// DistinctOnExists filters Location records to include only the latest per group
// of groupFields, ordered by orderField. Filters are propagated to the NOT EXISTS subquery.
func (_q *LocationQuery) DistinctOnExists(groupFields []string, orderField string, filters ...predicate.Location) *LocationQuery {
	_q.Where(func(s *sql.Selector) {
		t := sql.Table("locations").As("s2")
		conditions := make([]*sql.Predicate, 0, len(groupFields)+1)
		for _, f := range groupFields {
			conditions = append(conditions, sql.ColumnsEQ(t.C(f), s.C(f)))
		}
		conditions = append(conditions, sql.ColumnsGT(t.C(orderField), s.C(orderField)))
		sub := sql.SelectExpr(sql.Expr("1")).From(t).Where(sql.And(conditions...))
		for _, pf := range filters {
			pf(sub)
		}
		s.Where(sql.Not(sql.Exists(sub)))
	})
	return _q
}

// DistinctOnExists filters Role records to include only the latest per group
// of groupFields, ordered by orderField. Filters are propagated to the NOT EXISTS subquery.
func (_q *RoleQuery) DistinctOnExists(groupFields []string, orderField string, filters ...predicate.Role) *RoleQuery {
	_q.Where(func(s *sql.Selector) {
		t := sql.Table("roles").As("s2")
		conditions := make([]*sql.Predicate, 0, len(groupFields)+1)
		for _, f := range groupFields {
			conditions = append(conditions, sql.ColumnsEQ(t.C(f), s.C(f)))
		}
		conditions = append(conditions, sql.ColumnsGT(t.C(orderField), s.C(orderField)))
		sub := sql.SelectExpr(sql.Expr("1")).From(t).Where(sql.And(conditions...))
		for _, pf := range filters {
			pf(sub)
		}
		s.Where(sql.Not(sql.Exists(sub)))
	})
	return _q
}

// DistinctOnExists filters Tenant records to include only the latest per group
// of groupFields, ordered by orderField. Filters are propagated to the NOT EXISTS subquery.
func (_q *TenantQuery) DistinctOnExists(groupFields []string, orderField string, filters ...predicate.Tenant) *TenantQuery {
	_q.Where(func(s *sql.Selector) {
		t := sql.Table("tenants").As("s2")
		conditions := make([]*sql.Predicate, 0, len(groupFields)+1)
		for _, f := range groupFields {
			conditions = append(conditions, sql.ColumnsEQ(t.C(f), s.C(f)))
		}
		conditions = append(conditions, sql.ColumnsGT(t.C(orderField), s.C(orderField)))
		sub := sql.SelectExpr(sql.Expr("1")).From(t).Where(sql.And(conditions...))
		for _, pf := range filters {
			pf(sub)
		}
		s.Where(sql.Not(sql.Exists(sub)))
	})
	return _q
}

// DistinctOnExists filters User records to include only the latest per group
// of groupFields, ordered by orderField. Filters are propagated to the NOT EXISTS subquery.
func (_q *UserQuery) DistinctOnExists(groupFields []string, orderField string, filters ...predicate.User) *UserQuery {
	_q.Where(func(s *sql.Selector) {
		t := sql.Table("users").As("s2")
		conditions := make([]*sql.Predicate, 0, len(groupFields)+1)
		for _, f := range groupFields {
			conditions = append(conditions, sql.ColumnsEQ(t.C(f), s.C(f)))
		}
		conditions = append(conditions, sql.ColumnsGT(t.C(orderField), s.C(orderField)))
		sub := sql.SelectExpr(sql.Expr("1")).From(t).Where(sql.And(conditions...))
		for _, pf := range filters {
			pf(sub)
		}
		s.Where(sql.Not(sql.Exists(sub)))
	})
	return _q
}
