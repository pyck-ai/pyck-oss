// Code generated by ent, DO NOT EDIT.

package gen

import "context"

// AllPages paginates through all Collection_Movement records matching the current
// query predicates in pages of pageSize, collecting every row. This avoids the
// LimitMixin hard cap that silently truncates results when no explicit Limit is
// set.
//
// An ORDER BY id ASC is appended so pagination is deterministic — without it,
// Postgres LIMIT/OFFSET returns rows in unspecified order and pages can
// overlap or skip rows depending on the chosen plan.
func (_q *CollectionMovementQuery) AllPages(ctx context.Context, pageSize int) ([]*Collection_Movement, error) {
	var all []*Collection_Movement
	for offset := 0; ; offset += pageSize {
		page, err := _q.Clone().Order(Asc("id")).Limit(pageSize).Offset(offset).All(ctx)
		if err != nil {
			return nil, err
		}
		all = append(all, page...)
		if len(page) < pageSize {
			break
		}
	}
	return all, nil
}

// AllPages paginates through all EntityEventsOutbox records matching the current
// query predicates in pages of pageSize, collecting every row. This avoids the
// LimitMixin hard cap that silently truncates results when no explicit Limit is
// set.
//
// An ORDER BY id ASC is appended so pagination is deterministic — without it,
// Postgres LIMIT/OFFSET returns rows in unspecified order and pages can
// overlap or skip rows depending on the chosen plan.
func (_q *EntityEventsOutboxQuery) AllPages(ctx context.Context, pageSize int) ([]*EntityEventsOutbox, error) {
	var all []*EntityEventsOutbox
	for offset := 0; ; offset += pageSize {
		page, err := _q.Clone().Order(Asc("id")).Limit(pageSize).Offset(offset).All(ctx)
		if err != nil {
			return nil, err
		}
		all = append(all, page...)
		if len(page) < pageSize {
			break
		}
	}
	return all, nil
}

// AllPages paginates through all IdempotencyKey records matching the current
// query predicates in pages of pageSize, collecting every row. This avoids the
// LimitMixin hard cap that silently truncates results when no explicit Limit is
// set.
//
// An ORDER BY id ASC is appended so pagination is deterministic — without it,
// Postgres LIMIT/OFFSET returns rows in unspecified order and pages can
// overlap or skip rows depending on the chosen plan.
func (_q *IdempotencyKeyQuery) AllPages(ctx context.Context, pageSize int) ([]*IdempotencyKey, error) {
	var all []*IdempotencyKey
	for offset := 0; ; offset += pageSize {
		page, err := _q.Clone().Order(Asc("id")).Limit(pageSize).Offset(offset).All(ctx)
		if err != nil {
			return nil, err
		}
		all = append(all, page...)
		if len(page) < pageSize {
			break
		}
	}
	return all, nil
}

// AllPages paginates through all Item records matching the current
// query predicates in pages of pageSize, collecting every row. This avoids the
// LimitMixin hard cap that silently truncates results when no explicit Limit is
// set.
//
// An ORDER BY id ASC is appended so pagination is deterministic — without it,
// Postgres LIMIT/OFFSET returns rows in unspecified order and pages can
// overlap or skip rows depending on the chosen plan.
func (_q *ItemQuery) AllPages(ctx context.Context, pageSize int) ([]*Item, error) {
	var all []*Item
	for offset := 0; ; offset += pageSize {
		page, err := _q.Clone().Order(Asc("id")).Limit(pageSize).Offset(offset).All(ctx)
		if err != nil {
			return nil, err
		}
		all = append(all, page...)
		if len(page) < pageSize {
			break
		}
	}
	return all, nil
}

// AllPages paginates through all ItemMovement records matching the current
// query predicates in pages of pageSize, collecting every row. This avoids the
// LimitMixin hard cap that silently truncates results when no explicit Limit is
// set.
//
// An ORDER BY id ASC is appended so pagination is deterministic — without it,
// Postgres LIMIT/OFFSET returns rows in unspecified order and pages can
// overlap or skip rows depending on the chosen plan.
func (_q *ItemMovementQuery) AllPages(ctx context.Context, pageSize int) ([]*ItemMovement, error) {
	var all []*ItemMovement
	for offset := 0; ; offset += pageSize {
		page, err := _q.Clone().Order(Asc("id")).Limit(pageSize).Offset(offset).All(ctx)
		if err != nil {
			return nil, err
		}
		all = append(all, page...)
		if len(page) < pageSize {
			break
		}
	}
	return all, nil
}

// AllPages paginates through all ItemSet records matching the current
// query predicates in pages of pageSize, collecting every row. This avoids the
// LimitMixin hard cap that silently truncates results when no explicit Limit is
// set.
//
// An ORDER BY id ASC is appended so pagination is deterministic — without it,
// Postgres LIMIT/OFFSET returns rows in unspecified order and pages can
// overlap or skip rows depending on the chosen plan.
func (_q *ItemSetQuery) AllPages(ctx context.Context, pageSize int) ([]*ItemSet, error) {
	var all []*ItemSet
	for offset := 0; ; offset += pageSize {
		page, err := _q.Clone().Order(Asc("id")).Limit(pageSize).Offset(offset).All(ctx)
		if err != nil {
			return nil, err
		}
		all = append(all, page...)
		if len(page) < pageSize {
			break
		}
	}
	return all, nil
}

// AllPages paginates through all ReplenishmentOrder records matching the current
// query predicates in pages of pageSize, collecting every row. This avoids the
// LimitMixin hard cap that silently truncates results when no explicit Limit is
// set.
//
// An ORDER BY id ASC is appended so pagination is deterministic — without it,
// Postgres LIMIT/OFFSET returns rows in unspecified order and pages can
// overlap or skip rows depending on the chosen plan.
func (_q *ReplenishmentOrderQuery) AllPages(ctx context.Context, pageSize int) ([]*ReplenishmentOrder, error) {
	var all []*ReplenishmentOrder
	for offset := 0; ; offset += pageSize {
		page, err := _q.Clone().Order(Asc("id")).Limit(pageSize).Offset(offset).All(ctx)
		if err != nil {
			return nil, err
		}
		all = append(all, page...)
		if len(page) < pageSize {
			break
		}
	}
	return all, nil
}

// AllPages paginates through all ReplenishmentOrderItem records matching the current
// query predicates in pages of pageSize, collecting every row. This avoids the
// LimitMixin hard cap that silently truncates results when no explicit Limit is
// set.
//
// An ORDER BY id ASC is appended so pagination is deterministic — without it,
// Postgres LIMIT/OFFSET returns rows in unspecified order and pages can
// overlap or skip rows depending on the chosen plan.
func (_q *ReplenishmentOrderItemQuery) AllPages(ctx context.Context, pageSize int) ([]*ReplenishmentOrderItem, error) {
	var all []*ReplenishmentOrderItem
	for offset := 0; ; offset += pageSize {
		page, err := _q.Clone().Order(Asc("id")).Limit(pageSize).Offset(offset).All(ctx)
		if err != nil {
			return nil, err
		}
		all = append(all, page...)
		if len(page) < pageSize {
			break
		}
	}
	return all, nil
}

// AllPages paginates through all Repository records matching the current
// query predicates in pages of pageSize, collecting every row. This avoids the
// LimitMixin hard cap that silently truncates results when no explicit Limit is
// set.
//
// An ORDER BY id ASC is appended so pagination is deterministic — without it,
// Postgres LIMIT/OFFSET returns rows in unspecified order and pages can
// overlap or skip rows depending on the chosen plan.
func (_q *RepositoryQuery) AllPages(ctx context.Context, pageSize int) ([]*Repository, error) {
	var all []*Repository
	for offset := 0; ; offset += pageSize {
		page, err := _q.Clone().Order(Asc("id")).Limit(pageSize).Offset(offset).All(ctx)
		if err != nil {
			return nil, err
		}
		all = append(all, page...)
		if len(page) < pageSize {
			break
		}
	}
	return all, nil
}

// AllPages paginates through all RepositoryMovement records matching the current
// query predicates in pages of pageSize, collecting every row. This avoids the
// LimitMixin hard cap that silently truncates results when no explicit Limit is
// set.
//
// An ORDER BY id ASC is appended so pagination is deterministic — without it,
// Postgres LIMIT/OFFSET returns rows in unspecified order and pages can
// overlap or skip rows depending on the chosen plan.
func (_q *RepositoryMovementQuery) AllPages(ctx context.Context, pageSize int) ([]*RepositoryMovement, error) {
	var all []*RepositoryMovement
	for offset := 0; ; offset += pageSize {
		page, err := _q.Clone().Order(Asc("id")).Limit(pageSize).Offset(offset).All(ctx)
		if err != nil {
			return nil, err
		}
		all = append(all, page...)
		if len(page) < pageSize {
			break
		}
	}
	return all, nil
}

// AllPages paginates through all Stock records matching the current
// query predicates in pages of pageSize, collecting every row. This avoids the
// LimitMixin hard cap that silently truncates results when no explicit Limit is
// set.
//
// An ORDER BY id ASC is appended so pagination is deterministic — without it,
// Postgres LIMIT/OFFSET returns rows in unspecified order and pages can
// overlap or skip rows depending on the chosen plan.
func (_q *StockQuery) AllPages(ctx context.Context, pageSize int) ([]*Stock, error) {
	var all []*Stock
	for offset := 0; ; offset += pageSize {
		page, err := _q.Clone().Order(Asc("id")).Limit(pageSize).Offset(offset).All(ctx)
		if err != nil {
			return nil, err
		}
		all = append(all, page...)
		if len(page) < pageSize {
			break
		}
	}
	return all, nil
}

// AllPages paginates through all Transaction records matching the current
// query predicates in pages of pageSize, collecting every row. This avoids the
// LimitMixin hard cap that silently truncates results when no explicit Limit is
// set.
//
// An ORDER BY id ASC is appended so pagination is deterministic — without it,
// Postgres LIMIT/OFFSET returns rows in unspecified order and pages can
// overlap or skip rows depending on the chosen plan.
func (_q *TransactionQuery) AllPages(ctx context.Context, pageSize int) ([]*Transaction, error) {
	var all []*Transaction
	for offset := 0; ; offset += pageSize {
		page, err := _q.Clone().Order(Asc("id")).Limit(pageSize).Offset(offset).All(ctx)
		if err != nil {
			return nil, err
		}
		all = append(all, page...)
		if len(page) < pageSize {
			break
		}
	}
	return all, nil
}
