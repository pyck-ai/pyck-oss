// Code generated by ent, DO NOT EDIT.

package gen

import (
	"entgo.io/ent/dialect/sql"
	"github.com/pyck-ai/pyck/backend/inventory/ent/gen/predicate"
)

// DistinctOnExists filters Collection_Movement records to include only the latest per group
// of groupFields, ordered by orderField. Filters are propagated to the NOT EXISTS subquery.
func (_q *CollectionMovementQuery) DistinctOnExists(groupFields []string, orderField string, filters ...predicate.Collection_Movement) *CollectionMovementQuery {
	_q.Where(func(s *sql.Selector) {
		t := sql.Table("collection_movements").As("s2")
		conditions := make([]*sql.Predicate, 0, len(groupFields)+1)
		for _, f := range groupFields {
			conditions = append(conditions, sql.ColumnsEQ(t.C(f), s.C(f)))
		}
		conditions = append(conditions, sql.ColumnsGT(t.C(orderField), s.C(orderField)))
		sub := sql.SelectExpr(sql.Expr("1")).From(t).Where(sql.And(conditions...))
		for _, pf := range filters {
			pf(sub)
		}
		s.Where(sql.Not(sql.Exists(sub)))
	})
	return _q
}

// DistinctOnExists filters EntityEventsOutbox records to include only the latest per group
// of groupFields, ordered by orderField. Filters are propagated to the NOT EXISTS subquery.
func (_q *EntityEventsOutboxQuery) DistinctOnExists(groupFields []string, orderField string, filters ...predicate.EntityEventsOutbox) *EntityEventsOutboxQuery {
	_q.Where(func(s *sql.Selector) {
		t := sql.Table("event_outbox").As("s2")
		conditions := make([]*sql.Predicate, 0, len(groupFields)+1)
		for _, f := range groupFields {
			conditions = append(conditions, sql.ColumnsEQ(t.C(f), s.C(f)))
		}
		conditions = append(conditions, sql.ColumnsGT(t.C(orderField), s.C(orderField)))
		sub := sql.SelectExpr(sql.Expr("1")).From(t).Where(sql.And(conditions...))
		for _, pf := range filters {
			pf(sub)
		}
		s.Where(sql.Not(sql.Exists(sub)))
	})
	return _q
}

// DistinctOnExists filters IdempotencyKey records to include only the latest per group
// of groupFields, ordered by orderField. Filters are propagated to the NOT EXISTS subquery.
func (_q *IdempotencyKeyQuery) DistinctOnExists(groupFields []string, orderField string, filters ...predicate.IdempotencyKey) *IdempotencyKeyQuery {
	_q.Where(func(s *sql.Selector) {
		t := sql.Table("idempotency_keys").As("s2")
		conditions := make([]*sql.Predicate, 0, len(groupFields)+1)
		for _, f := range groupFields {
			conditions = append(conditions, sql.ColumnsEQ(t.C(f), s.C(f)))
		}
		conditions = append(conditions, sql.ColumnsGT(t.C(orderField), s.C(orderField)))
		sub := sql.SelectExpr(sql.Expr("1")).From(t).Where(sql.And(conditions...))
		for _, pf := range filters {
			pf(sub)
		}
		s.Where(sql.Not(sql.Exists(sub)))
	})
	return _q
}

// DistinctOnExists filters Item records to include only the latest per group
// of groupFields, ordered by orderField. Filters are propagated to the NOT EXISTS subquery.
func (_q *ItemQuery) DistinctOnExists(groupFields []string, orderField string, filters ...predicate.Item) *ItemQuery {
	_q.Where(func(s *sql.Selector) {
		t := sql.Table("items").As("s2")
		conditions := make([]*sql.Predicate, 0, len(groupFields)+1)
		for _, f := range groupFields {
			conditions = append(conditions, sql.ColumnsEQ(t.C(f), s.C(f)))
		}
		conditions = append(conditions, sql.ColumnsGT(t.C(orderField), s.C(orderField)))
		sub := sql.SelectExpr(sql.Expr("1")).From(t).Where(sql.And(conditions...))
		for _, pf := range filters {
			pf(sub)
		}
		s.Where(sql.Not(sql.Exists(sub)))
	})
	return _q
}

// DistinctOnExists filters ItemMovement records to include only the latest per group
// of groupFields, ordered by orderField. Filters are propagated to the NOT EXISTS subquery.
func (_q *ItemMovementQuery) DistinctOnExists(groupFields []string, orderField string, filters ...predicate.ItemMovement) *ItemMovementQuery {
	_q.Where(func(s *sql.Selector) {
		t := sql.Table("item_movements").As("s2")
		conditions := make([]*sql.Predicate, 0, len(groupFields)+1)
		for _, f := range groupFields {
			conditions = append(conditions, sql.ColumnsEQ(t.C(f), s.C(f)))
		}
		conditions = append(conditions, sql.ColumnsGT(t.C(orderField), s.C(orderField)))
		sub := sql.SelectExpr(sql.Expr("1")).From(t).Where(sql.And(conditions...))
		for _, pf := range filters {
			pf(sub)
		}
		s.Where(sql.Not(sql.Exists(sub)))
	})
	return _q
}

// DistinctOnExists filters ItemSet records to include only the latest per group
// of groupFields, ordered by orderField. Filters are propagated to the NOT EXISTS subquery.
func (_q *ItemSetQuery) DistinctOnExists(groupFields []string, orderField string, filters ...predicate.ItemSet) *ItemSetQuery {
	_q.Where(func(s *sql.Selector) {
		t := sql.Table("item_sets").As("s2")
		conditions := make([]*sql.Predicate, 0, len(groupFields)+1)
		for _, f := range groupFields {
			conditions = append(conditions, sql.ColumnsEQ(t.C(f), s.C(f)))
		}
		conditions = append(conditions, sql.ColumnsGT(t.C(orderField), s.C(orderField)))
		sub := sql.SelectExpr(sql.Expr("1")).From(t).Where(sql.And(conditions...))
		for _, pf := range filters {
			pf(sub)
		}
		s.Where(sql.Not(sql.Exists(sub)))
	})
	return _q
}

// DistinctOnExists filters ReplenishmentOrder records to include only the latest per group
// of groupFields, ordered by orderField. Filters are propagated to the NOT EXISTS subquery.
func (_q *ReplenishmentOrderQuery) DistinctOnExists(groupFields []string, orderField string, filters ...predicate.ReplenishmentOrder) *ReplenishmentOrderQuery {
	_q.Where(func(s *sql.Selector) {
		t := sql.Table("replenishment_orders").As("s2")
		conditions := make([]*sql.Predicate, 0, len(groupFields)+1)
		for _, f := range groupFields {
			conditions = append(conditions, sql.ColumnsEQ(t.C(f), s.C(f)))
		}
		conditions = append(conditions, sql.ColumnsGT(t.C(orderField), s.C(orderField)))
		sub := sql.SelectExpr(sql.Expr("1")).From(t).Where(sql.And(conditions...))
		for _, pf := range filters {
			pf(sub)
		}
		s.Where(sql.Not(sql.Exists(sub)))
	})
	return _q
}

// DistinctOnExists filters ReplenishmentOrderItem records to include only the latest per group
// of groupFields, ordered by orderField. Filters are propagated to the NOT EXISTS subquery.
func (_q *ReplenishmentOrderItemQuery) DistinctOnExists(groupFields []string, orderField string, filters ...predicate.ReplenishmentOrderItem) *ReplenishmentOrderItemQuery {
	_q.Where(func(s *sql.Selector) {
		t := sql.Table("replenishment_order_items").As("s2")
		conditions := make([]*sql.Predicate, 0, len(groupFields)+1)
		for _, f := range groupFields {
			conditions = append(conditions, sql.ColumnsEQ(t.C(f), s.C(f)))
		}
		conditions = append(conditions, sql.ColumnsGT(t.C(orderField), s.C(orderField)))
		sub := sql.SelectExpr(sql.Expr("1")).From(t).Where(sql.And(conditions...))
		for _, pf := range filters {
			pf(sub)
		}
		s.Where(sql.Not(sql.Exists(sub)))
	})
	return _q
}

// DistinctOnExists filters Repository records to include only the latest per group
// of groupFields, ordered by orderField. Filters are propagated to the NOT EXISTS subquery.
func (_q *RepositoryQuery) DistinctOnExists(groupFields []string, orderField string, filters ...predicate.Repository) *RepositoryQuery {
	_q.Where(func(s *sql.Selector) {
		t := sql.Table("repositories").As("s2")
		conditions := make([]*sql.Predicate, 0, len(groupFields)+1)
		for _, f := range groupFields {
			conditions = append(conditions, sql.ColumnsEQ(t.C(f), s.C(f)))
		}
		conditions = append(conditions, sql.ColumnsGT(t.C(orderField), s.C(orderField)))
		sub := sql.SelectExpr(sql.Expr("1")).From(t).Where(sql.And(conditions...))
		for _, pf := range filters {
			pf(sub)
		}
		s.Where(sql.Not(sql.Exists(sub)))
	})
	return _q
}

// DistinctOnExists filters RepositoryMovement records to include only the latest per group
// of groupFields, ordered by orderField. Filters are propagated to the NOT EXISTS subquery.
func (_q *RepositoryMovementQuery) DistinctOnExists(groupFields []string, orderField string, filters ...predicate.RepositoryMovement) *RepositoryMovementQuery {
	_q.Where(func(s *sql.Selector) {
		t := sql.Table("repository_movements").As("s2")
		conditions := make([]*sql.Predicate, 0, len(groupFields)+1)
		for _, f := range groupFields {
			conditions = append(conditions, sql.ColumnsEQ(t.C(f), s.C(f)))
		}
		conditions = append(conditions, sql.ColumnsGT(t.C(orderField), s.C(orderField)))
		sub := sql.SelectExpr(sql.Expr("1")).From(t).Where(sql.And(conditions...))
		for _, pf := range filters {
			pf(sub)
		}
		s.Where(sql.Not(sql.Exists(sub)))
	})
	return _q
}

// DistinctOnExists filters Stock records to include only the latest per group
// of groupFields, ordered by orderField. Filters are propagated to the NOT EXISTS subquery.
func (_q *StockQuery) DistinctOnExists(groupFields []string, orderField string, filters ...predicate.Stock) *StockQuery {
	_q.Where(func(s *sql.Selector) {
		t := sql.Table("stocks").As("s2")
		conditions := make([]*sql.Predicate, 0, len(groupFields)+1)
		for _, f := range groupFields {
			conditions = append(conditions, sql.ColumnsEQ(t.C(f), s.C(f)))
		}
		conditions = append(conditions, sql.ColumnsGT(t.C(orderField), s.C(orderField)))
		sub := sql.SelectExpr(sql.Expr("1")).From(t).Where(sql.And(conditions...))
		for _, pf := range filters {
			pf(sub)
		}
		s.Where(sql.Not(sql.Exists(sub)))
	})
	return _q
}

// DistinctOnExists filters Transaction records to include only the latest per group
// of groupFields, ordered by orderField. Filters are propagated to the NOT EXISTS subquery.
func (_q *TransactionQuery) DistinctOnExists(groupFields []string, orderField string, filters ...predicate.Transaction) *TransactionQuery {
	_q.Where(func(s *sql.Selector) {
		t := sql.Table("transactions").As("s2")
		conditions := make([]*sql.Predicate, 0, len(groupFields)+1)
		for _, f := range groupFields {
			conditions = append(conditions, sql.ColumnsEQ(t.C(f), s.C(f)))
		}
		conditions = append(conditions, sql.ColumnsGT(t.C(orderField), s.C(orderField)))
		sub := sql.SelectExpr(sql.Expr("1")).From(t).Where(sql.And(conditions...))
		for _, pf := range filters {
			pf(sub)
		}
		s.Where(sql.Not(sql.Exists(sub)))
	})
	return _q
}
