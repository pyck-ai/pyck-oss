// Code generated by ent, DO NOT EDIT.

package gen

import (
	"context"

	"github.com/google/uuid"
)

// PagedIDs paginates through the ids of all Collection_Movement records matching
// the current query predicates in pages of pageSize, collecting every id. It is
// the .IDs(ctx) counterpart of AllPages: use it instead of a bare .IDs(ctx),
// which the LimitMixin hard cap silently truncates when no explicit Limit is
// set. Unlike .Select(FieldID).AllPages(), it scans ids directly without
// allocating a Collection_Movement struct per row.
//
// An ORDER BY id ASC is appended so pagination is deterministic — without it,
// Postgres LIMIT/OFFSET returns rows in unspecified order and pages can
// overlap or skip rows depending on the chosen plan.
func (_q *CollectionMovementQuery) PagedIDs(ctx context.Context, pageSize int) ([]uuid.UUID, error) {
	var all []uuid.UUID
	for offset := 0; ; offset += pageSize {
		page, err := _q.Clone().Order(Asc("id")).Limit(pageSize).Offset(offset).IDs(ctx)
		if err != nil {
			return nil, err
		}
		all = append(all, page...)
		if len(page) < pageSize {
			break
		}
	}
	return all, nil
}

// PagedIDs paginates through the ids of all EntityEventsOutbox records matching
// the current query predicates in pages of pageSize, collecting every id. It is
// the .IDs(ctx) counterpart of AllPages: use it instead of a bare .IDs(ctx),
// which the LimitMixin hard cap silently truncates when no explicit Limit is
// set. Unlike .Select(FieldID).AllPages(), it scans ids directly without
// allocating a EntityEventsOutbox struct per row.
//
// An ORDER BY id ASC is appended so pagination is deterministic — without it,
// Postgres LIMIT/OFFSET returns rows in unspecified order and pages can
// overlap or skip rows depending on the chosen plan.
func (_q *EntityEventsOutboxQuery) PagedIDs(ctx context.Context, pageSize int) ([]uuid.UUID, error) {
	var all []uuid.UUID
	for offset := 0; ; offset += pageSize {
		page, err := _q.Clone().Order(Asc("id")).Limit(pageSize).Offset(offset).IDs(ctx)
		if err != nil {
			return nil, err
		}
		all = append(all, page...)
		if len(page) < pageSize {
			break
		}
	}
	return all, nil
}

// PagedIDs paginates through the ids of all IdempotencyKey records matching
// the current query predicates in pages of pageSize, collecting every id. It is
// the .IDs(ctx) counterpart of AllPages: use it instead of a bare .IDs(ctx),
// which the LimitMixin hard cap silently truncates when no explicit Limit is
// set. Unlike .Select(FieldID).AllPages(), it scans ids directly without
// allocating a IdempotencyKey struct per row.
//
// An ORDER BY id ASC is appended so pagination is deterministic — without it,
// Postgres LIMIT/OFFSET returns rows in unspecified order and pages can
// overlap or skip rows depending on the chosen plan.
func (_q *IdempotencyKeyQuery) PagedIDs(ctx context.Context, pageSize int) ([]uuid.UUID, error) {
	var all []uuid.UUID
	for offset := 0; ; offset += pageSize {
		page, err := _q.Clone().Order(Asc("id")).Limit(pageSize).Offset(offset).IDs(ctx)
		if err != nil {
			return nil, err
		}
		all = append(all, page...)
		if len(page) < pageSize {
			break
		}
	}
	return all, nil
}

// PagedIDs paginates through the ids of all Item records matching
// the current query predicates in pages of pageSize, collecting every id. It is
// the .IDs(ctx) counterpart of AllPages: use it instead of a bare .IDs(ctx),
// which the LimitMixin hard cap silently truncates when no explicit Limit is
// set. Unlike .Select(FieldID).AllPages(), it scans ids directly without
// allocating a Item struct per row.
//
// An ORDER BY id ASC is appended so pagination is deterministic — without it,
// Postgres LIMIT/OFFSET returns rows in unspecified order and pages can
// overlap or skip rows depending on the chosen plan.
func (_q *ItemQuery) PagedIDs(ctx context.Context, pageSize int) ([]uuid.UUID, error) {
	var all []uuid.UUID
	for offset := 0; ; offset += pageSize {
		page, err := _q.Clone().Order(Asc("id")).Limit(pageSize).Offset(offset).IDs(ctx)
		if err != nil {
			return nil, err
		}
		all = append(all, page...)
		if len(page) < pageSize {
			break
		}
	}
	return all, nil
}

// PagedIDs paginates through the ids of all ItemMovement records matching
// the current query predicates in pages of pageSize, collecting every id. It is
// the .IDs(ctx) counterpart of AllPages: use it instead of a bare .IDs(ctx),
// which the LimitMixin hard cap silently truncates when no explicit Limit is
// set. Unlike .Select(FieldID).AllPages(), it scans ids directly without
// allocating a ItemMovement struct per row.
//
// An ORDER BY id ASC is appended so pagination is deterministic — without it,
// Postgres LIMIT/OFFSET returns rows in unspecified order and pages can
// overlap or skip rows depending on the chosen plan.
func (_q *ItemMovementQuery) PagedIDs(ctx context.Context, pageSize int) ([]uuid.UUID, error) {
	var all []uuid.UUID
	for offset := 0; ; offset += pageSize {
		page, err := _q.Clone().Order(Asc("id")).Limit(pageSize).Offset(offset).IDs(ctx)
		if err != nil {
			return nil, err
		}
		all = append(all, page...)
		if len(page) < pageSize {
			break
		}
	}
	return all, nil
}

// PagedIDs paginates through the ids of all ItemSet records matching
// the current query predicates in pages of pageSize, collecting every id. It is
// the .IDs(ctx) counterpart of AllPages: use it instead of a bare .IDs(ctx),
// which the LimitMixin hard cap silently truncates when no explicit Limit is
// set. Unlike .Select(FieldID).AllPages(), it scans ids directly without
// allocating a ItemSet struct per row.
//
// An ORDER BY id ASC is appended so pagination is deterministic — without it,
// Postgres LIMIT/OFFSET returns rows in unspecified order and pages can
// overlap or skip rows depending on the chosen plan.
func (_q *ItemSetQuery) PagedIDs(ctx context.Context, pageSize int) ([]uuid.UUID, error) {
	var all []uuid.UUID
	for offset := 0; ; offset += pageSize {
		page, err := _q.Clone().Order(Asc("id")).Limit(pageSize).Offset(offset).IDs(ctx)
		if err != nil {
			return nil, err
		}
		all = append(all, page...)
		if len(page) < pageSize {
			break
		}
	}
	return all, nil
}

// PagedIDs paginates through the ids of all ReplenishmentOrder records matching
// the current query predicates in pages of pageSize, collecting every id. It is
// the .IDs(ctx) counterpart of AllPages: use it instead of a bare .IDs(ctx),
// which the LimitMixin hard cap silently truncates when no explicit Limit is
// set. Unlike .Select(FieldID).AllPages(), it scans ids directly without
// allocating a ReplenishmentOrder struct per row.
//
// An ORDER BY id ASC is appended so pagination is deterministic — without it,
// Postgres LIMIT/OFFSET returns rows in unspecified order and pages can
// overlap or skip rows depending on the chosen plan.
func (_q *ReplenishmentOrderQuery) PagedIDs(ctx context.Context, pageSize int) ([]uuid.UUID, error) {
	var all []uuid.UUID
	for offset := 0; ; offset += pageSize {
		page, err := _q.Clone().Order(Asc("id")).Limit(pageSize).Offset(offset).IDs(ctx)
		if err != nil {
			return nil, err
		}
		all = append(all, page...)
		if len(page) < pageSize {
			break
		}
	}
	return all, nil
}

// PagedIDs paginates through the ids of all ReplenishmentOrderItem records matching
// the current query predicates in pages of pageSize, collecting every id. It is
// the .IDs(ctx) counterpart of AllPages: use it instead of a bare .IDs(ctx),
// which the LimitMixin hard cap silently truncates when no explicit Limit is
// set. Unlike .Select(FieldID).AllPages(), it scans ids directly without
// allocating a ReplenishmentOrderItem struct per row.
//
// An ORDER BY id ASC is appended so pagination is deterministic — without it,
// Postgres LIMIT/OFFSET returns rows in unspecified order and pages can
// overlap or skip rows depending on the chosen plan.
func (_q *ReplenishmentOrderItemQuery) PagedIDs(ctx context.Context, pageSize int) ([]uuid.UUID, error) {
	var all []uuid.UUID
	for offset := 0; ; offset += pageSize {
		page, err := _q.Clone().Order(Asc("id")).Limit(pageSize).Offset(offset).IDs(ctx)
		if err != nil {
			return nil, err
		}
		all = append(all, page...)
		if len(page) < pageSize {
			break
		}
	}
	return all, nil
}

// PagedIDs paginates through the ids of all Repository records matching
// the current query predicates in pages of pageSize, collecting every id. It is
// the .IDs(ctx) counterpart of AllPages: use it instead of a bare .IDs(ctx),
// which the LimitMixin hard cap silently truncates when no explicit Limit is
// set. Unlike .Select(FieldID).AllPages(), it scans ids directly without
// allocating a Repository struct per row.
//
// An ORDER BY id ASC is appended so pagination is deterministic — without it,
// Postgres LIMIT/OFFSET returns rows in unspecified order and pages can
// overlap or skip rows depending on the chosen plan.
func (_q *RepositoryQuery) PagedIDs(ctx context.Context, pageSize int) ([]uuid.UUID, error) {
	var all []uuid.UUID
	for offset := 0; ; offset += pageSize {
		page, err := _q.Clone().Order(Asc("id")).Limit(pageSize).Offset(offset).IDs(ctx)
		if err != nil {
			return nil, err
		}
		all = append(all, page...)
		if len(page) < pageSize {
			break
		}
	}
	return all, nil
}

// PagedIDs paginates through the ids of all RepositoryMovement records matching
// the current query predicates in pages of pageSize, collecting every id. It is
// the .IDs(ctx) counterpart of AllPages: use it instead of a bare .IDs(ctx),
// which the LimitMixin hard cap silently truncates when no explicit Limit is
// set. Unlike .Select(FieldID).AllPages(), it scans ids directly without
// allocating a RepositoryMovement struct per row.
//
// An ORDER BY id ASC is appended so pagination is deterministic — without it,
// Postgres LIMIT/OFFSET returns rows in unspecified order and pages can
// overlap or skip rows depending on the chosen plan.
func (_q *RepositoryMovementQuery) PagedIDs(ctx context.Context, pageSize int) ([]uuid.UUID, error) {
	var all []uuid.UUID
	for offset := 0; ; offset += pageSize {
		page, err := _q.Clone().Order(Asc("id")).Limit(pageSize).Offset(offset).IDs(ctx)
		if err != nil {
			return nil, err
		}
		all = append(all, page...)
		if len(page) < pageSize {
			break
		}
	}
	return all, nil
}

// PagedIDs paginates through the ids of all Stock records matching
// the current query predicates in pages of pageSize, collecting every id. It is
// the .IDs(ctx) counterpart of AllPages: use it instead of a bare .IDs(ctx),
// which the LimitMixin hard cap silently truncates when no explicit Limit is
// set. Unlike .Select(FieldID).AllPages(), it scans ids directly without
// allocating a Stock struct per row.
//
// An ORDER BY id ASC is appended so pagination is deterministic — without it,
// Postgres LIMIT/OFFSET returns rows in unspecified order and pages can
// overlap or skip rows depending on the chosen plan.
func (_q *StockQuery) PagedIDs(ctx context.Context, pageSize int) ([]uuid.UUID, error) {
	var all []uuid.UUID
	for offset := 0; ; offset += pageSize {
		page, err := _q.Clone().Order(Asc("id")).Limit(pageSize).Offset(offset).IDs(ctx)
		if err != nil {
			return nil, err
		}
		all = append(all, page...)
		if len(page) < pageSize {
			break
		}
	}
	return all, nil
}

// PagedIDs paginates through the ids of all Transaction records matching
// the current query predicates in pages of pageSize, collecting every id. It is
// the .IDs(ctx) counterpart of AllPages: use it instead of a bare .IDs(ctx),
// which the LimitMixin hard cap silently truncates when no explicit Limit is
// set. Unlike .Select(FieldID).AllPages(), it scans ids directly without
// allocating a Transaction struct per row.
//
// An ORDER BY id ASC is appended so pagination is deterministic — without it,
// Postgres LIMIT/OFFSET returns rows in unspecified order and pages can
// overlap or skip rows depending on the chosen plan.
func (_q *TransactionQuery) PagedIDs(ctx context.Context, pageSize int) ([]uuid.UUID, error) {
	var all []uuid.UUID
	for offset := 0; ; offset += pageSize {
		page, err := _q.Clone().Order(Asc("id")).Limit(pageSize).Offset(offset).IDs(ctx)
		if err != nil {
			return nil, err
		}
		all = append(all, page...)
		if len(page) < pageSize {
			break
		}
	}
	return all, nil
}
